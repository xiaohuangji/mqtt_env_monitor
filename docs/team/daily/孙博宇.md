# 孙博宇每日总结

## 记录模板

### 日期：____年__月__日

1. 今日主要工作内容
2. 本人完成的具体任务
3. 遇到的问题
4. 解决方法
5. 今日成果
6. 明日计划

# 课程设计日志

## 日期

2026 年 6 月 8 日

## 今日主要工作内容

上午上课时，认真听取了老师对课程设计要求的讲解，了解了本次课程设计的整体安排、工作日进度要求、阶段检查要求、报告结构、答辩方式和测试证据要求。今天主要围绕课程设计中硬件部分的任务进行了学习和初步规划。通过小组讨论，明确本人主要负责 ESP32 硬件节点相关内容，包括后续传感器选型、节点接线、数据采集和数据上传流程设计等工作。

在了解项目整体思路后，我重点学习了 ESP32 在本系统中的作用。ESP32 作为硬件采集节点，需要连接温湿度、光照、噪声等传感器，完成环境数据采集，并通过无线网络将数据上传到 MQTT Broker。相比 PC 模拟节点，ESP32 节点更接近真实物联网设备，因此后续需要更多考虑硬件连接、传感器读取稳定性和实际运行环境中的异常情况。

今天还初步了解了 MQTT 协议在硬件节点中的使用方式。ESP32 节点采集到数据后，并不是直接显示数据，而是需要按照统一的 Topic 和消息格式发布到 MQTT Broker，再由监控端订阅显示。因此，硬件部分的程序设计需要和前期制定的 MQTT 主题与消息格式保持一致。

同时，我对 ESP32 常用接口进行了初步学习，了解了数字引脚、模拟输入引脚、I2C 通信引脚等基本用途，为后续连接 DHT 温湿度传感器、BH1750 光照传感器和声音传感器做准备。

## 本人完成的具体任务

1. 明确本人在小组中主要负责硬件侧相关工作。
2. 初步学习 ESP32 在环境监测系统中的作用。
3. 了解 ESP32 通过 Wi-Fi 与 MQTT Broker 通信的基本流程。
4. 初步学习传感器数据采集的实现思路。
5. 了解 ESP32 常用引脚类型，为后续传感器接线做准备。
6. 初步思考硬件节点可能采集的数据类型，包括温度、湿度、光照和噪声。
7. 开始构思 ESP32 硬件节点设计初稿的内容方向。

硬件部分设计思路：硬件部分计划以 ESP32 作为核心控制板，通过外接传感器采集环境数据。第一版可以先实现基础环境数据采集，例如温度、湿度、光照和噪声。

ESP32 节点后续需要完成的基本流程为：

```text
读取传感器数据
  ↓
整理为统一数据格式
  ↓
连接 Wi-Fi
  ↓
发布到 MQTT Broker
  ↓
供 Web 监控端订阅显示
```

目前初步考虑使用 DHT11 / DHT22 采集温湿度，使用 BH1750 或光照模块采集光照，使用声音传感器模块采集噪声。具体传感器型号和接线方案还需要根据后续领取到的硬件器材进一步确定。

## 遇到的问题

今天主要的问题是对硬件节点的具体实现还不够熟悉，尤其是 ESP32 与不同传感器之间的连接方式、数据读取方式以及后续如何将采集结果封装为 MQTT 消息。

## 解决方法

针对 ESP32 和传感器连接不熟悉的问题，通过查询相关资料或者利用 AI 工具，先从 ESP32 常用引脚和常见传感器接口入手进行学习。

## 今日成果

1. 明确了硬件部分的个人任务方向。
2. 初步了解了 ESP32 硬件节点在系统中的作用。
3. 初步理解了 ESP32 节点采集数据并上传 MQTT Broker 的流程。
4. 对温湿度、光照、噪声等传感器的使用方向有了初步认识。
5. 为后续编写 ESP32 硬件节点设计初稿做了准备。

## 今日收获

通过今天的学习，我认识到硬件部分不仅是简单地把传感器接到 ESP32 上，还需要考虑传感器数据能否稳定读取、数据格式是否与软件端一致、节点是否能长期稳定上传数据等问题。

硬件节点是整个环境监测系统中真实数据的来源，后续如果硬件采集不稳定，会直接影响 Web 端显示和系统测试结果。因此，在后续设计中需要同时关注功能实现和运行可靠性。

## 明日计划

明天计划继续完善 ESP32 硬件节点设计，重点梳理硬件节点组成、传感器选型、接线方案和数据采集上传流程，为后续领取硬件器材后连接物理线路和编写 ESP32 程序做准备。

此外，按照课设要求还应着手分析 ESP32 节点在真实运行环境中可能遇到的工程问题，并提出相应的处理思路，用于后续的可靠性分析、工程测试等。


## 日期

2026 年 6 月 9 日

## 课设题目

基于 MQTT 的多节点环境监测系统设计

## 今日工作内容

今天主要完成了 ESP32 硬件节点相关内容的整理和补充。

首先，根据前面已经确定的 MQTT 主题与消息格式，对 ESP32 硬件节点文档进行了调整。明确 ESP32 节点需要按照统一的 Topic 格式 `env_monitor/{node_id}/{data_type}` 发布数据，并使用统一的 JSON 字段，包括 `node_id`、`data_type`、`value`、`unit`、`timestamp` 和 `seq`。同时，对温度、湿度、光照和噪声等数据类型及其单位进行了统一。

其次，整理了 ESP32 硬件节点的基本设计内容，包括节点组成、传感器选型和接线方案。第一版硬件节点计划使用 DHT11 / DHT22 采集温湿度，使用 BH1750 采集光照，使用噪声传感器模块采集环境噪声。根据项目节点编号规则，ESP32 硬件节点暂定为 `node03`。

之后，对 ESP32 节点在真实运行环境中可能遇到的问题进行了分析，主要包括 Wi-Fi 断开、MQTT Broker 连接失败、传感器数据异常、功耗、传感器精度和长时间运行稳定性等问题。针对这些问题，初步提出了 Wi-Fi 自动重连、MQTT 自动重连、节点心跳机制、QoS 选择和数据异常处理等工程化设计思路。

最后，整理了后续测试验证方向，包括重连时间测试、消息到达率测试、延迟测试、数据异常处理测试和连续运行稳定性测试。这些内容可以为后续课程设计报告中的可靠性分析和测试方案提供参考。

## 今日完成成果

1. 完成 ESP32 硬件节点接线与采集流程文档的精简整理。
2. 明确 ESP32 节点的传感器选型和接线方案。
3. 统一 ESP32 节点使用的 Topic、数据类型、单位和 JSON 字段。
4. 完成 ESP32 节点工程化分析初稿。
5. 初步提出重连机制、心跳机制、QoS 选择和数据异常处理方法。
6. 整理后续测试验证思路。

## 遇到的问题

在噪声传感器设计中存在一定问题。普通模拟声音传感器通常只能输出 ADC 原始值，不能直接得到准确的分贝值。而项目消息格式中规定噪声单位为 `dB`，因此后续需要考虑选用支持分贝检测的传感器模块，或者在程序中进行近似换算，并在报告中说明其精度限制。

## 解决方法

针对噪声数据单位问题，在工程化分析文档中说明了普通声音传感器的局限性，并提出第一版可以先作为近似噪声强度采集。后续若需要更准确的数据，应更换支持分贝检测的模块或进行实验标定。

## 今日收获

通过今天的工作，进一步明确了 ESP32 节点不仅要实现数据采集和 MQTT 发布，还需要考虑真实运行环境下的可靠性问题。Wi-Fi 重连、MQTT 重连、心跳机制和数据异常处理能够提高节点的稳定性和可维护性。

## 下一步计划

下一步将领取课程设计所需的硬件器材，并按照已规划的 ESP32 节点结构完成传感器接线。在此基础上，进一步编写数据采集程序，实现传感器数据读取、JSON 数据封装和串口调试输出，并对硬件部分的基础功能进行初步检测与调试。


## 日期

2026 年 6 月 10 日


## 今日主要工作内容

今天的课设任务主要围绕 ESP32 硬件节点展开。目前已有的硬件器材包括 MQ-2 气敏传感器模块、MH-RD 雨滴传感器模块、FC-28 土壤湿度传感器模块和 ESP32 开发板。今天的工作重点是完成这些传感器与 ESP32 的基础接线，并编写模块化采集程序，实现多类环境数据的读取、数据封装和串口输出，为后续接入 MQTT 数据上传流程提供硬件采集基础。

首先，对当前已有的传感器模块进行了整理和确认。MQ-2 气敏传感器用于采集气体相关模拟量数据，MH-RD 雨滴传感器用于检测雨滴或水滴状态，FC-28 土壤湿度传感器用于采集土壤湿度相关模拟量数据。由于本次任务主要是完成基础采集和检测，因此暂时不接入其他传感器，也不进行 MQTT 实际上传，只通过串口监视器观察采集结果。

在硬件连接方面，结合 ESP32 开发板的实际型号和引脚情况，对三个传感器的接口进行了重新设计。最初参考了普通 ESP32 的部分引脚规划，但后续确认使用的是 ESP-C3-32S 开发板，因此不能直接使用普通 ESP32 的 GPIO32、GPIO33、GPIO35 等引脚。根据 ESP-C3-32S 的可用引脚情况，重新规划为：MQ-2 气敏传感器 AO 接 GPIO0，MH-RD 雨滴传感器 AO 接 GPIO1，FC-28 土壤湿度传感器 AO 接 GPIO2。三个传感器均采用 VCC 接 3.3V、GND 接 GND、AO 接对应 ADC 引脚的方式进行基础连接。为降低调试复杂度，本次优先使用模拟量接口 AO 进行采集，DO 数字量接口暂不接入。

在软件实现方面，按照已有的 PlatformIO 工程结构，对传感器采集程序进行了模块化设计。原有工程中已经包含 `config.h`、`sensor_data.h`、`mqtt_message.h`、`mqtt_message.cpp` 和 `main.cpp` 等文件，同时保留了 DHT22、光照传感器和噪声传感器相关模块。由于这些传感器当前暂未接入，因此本次没有在主程序中调用。针对今天实际使用的三个传感器，新增并编写了 MQ-2 气敏传感器模块、雨滴传感器模块和土壤湿度传感器模块，使每类传感器的数据采集逻辑分别独立实现，便于后续维护和扩展。

在数据封装方面，继续参考前期已经确定的 MQTT 主题与消息格式设计。虽然今天没有连接 WiFi，也没有进行 MQTT 发布，但为了保证后续能够顺利接入 MQTT 上传流程，串口输出的数据格式仍然按照项目规定进行设计。Topic 采用 `env_monitor/{node_id}/{data_type}` 的结构，JSON 消息字段保持为 `node_id`、`data_type`、`value`、`unit`、`timestamp` 和 `seq`。当前 `timestamp` 暂时使用程序中设置的固定格式时间或调试时间，后续接入网络时间同步后再替换为真实时间。这样可以先验证 JSON 字段名称、大小写、数据类型和整体结构是否符合项目协议要求。

在主程序中，完成了三个传感器模块的统一初始化和循环调用。程序运行后，ESP32 会按照设定周期依次读取 MQ-2、MH-RD 和 FC-28 的模拟量数据，并将采集结果封装为统一格式后通过串口输出。串口监视器中能够显示各类环境数据对应的 Topic 和 Payload，便于观察传感器数值变化和检查消息格式是否正确。

## 今日完成情况

1. 完成 ESP32 与 MQ-2 气敏传感器模块的基础连接设计。
2. 完成 ESP32 与 MH-RD 雨滴传感器模块的基础连接设计。
3. 完成 ESP32 与 FC-28 土壤湿度传感器模块的基础连接设计。
4. 完成 ESP32 对三个传感器模拟量数据的读取程序编写。
5. 完成传感器采集程序的模块化拆分，使 MQ-2、雨滴传感器和土壤湿度传感器分别具有独立的头文件和源文件。
6. 完成主程序对各传感器模块的统一调用。
7. 完成采集数据的基础封装，并通过串口输出符合项目要求的 Topic 和 JSON Payload。
8. 完成 PlatformIO 工程配置调整，使程序能够适配 ESP-C3-32S 开发板进行编译和烧录。
9. 对串口输出结果进行了初步观察，为后续传感器实测和 MQTT 发布功能打下基础。

## 今日遇到的问题

1. **ESP32 开发板型号与原引脚规划不一致**

   在前期设计中，曾参考普通 ESP32 开发板的引脚表，将 MQ-2、雨滴传感器和土壤湿度传感器分别规划到 GPIO32、GPIO33 和 GPIO35。但实际使用的开发板为 ESP-C3-32S，该型号并没有这些引脚，若继续使用原规划会导致程序与硬件连接不匹配。

2. **传感器模块接口选择存在取舍**

   MQ-2、MH-RD 和 FC-28 模块通常同时具有 AO 和 DO 接口。AO 输出模拟量，能够反映传感器检测值的连续变化；DO 输出数字量，通常只表示是否超过模块电位器设定的阈值。如果同时接入 AO 和 DO，会增加引脚占用和调试复杂度。

3. **MQTT JSON 字段格式需要严格统一**

   在调试过程中，为了观察传感器状态，曾考虑加入 `status`、`analog_value`、`digital_value` 等辅助字段。但根据前期《MQTT 主题与消息格式设计》，正式 JSON Payload 应统一使用 `node_id`、`data_type`、`value`、`unit`、`timestamp` 和 `seq` 这些字段。如果随意增加字段，可能会影响 Web 监控端后续解析。

4. **PlatformIO 中 ESP32-C3 串口配置问题**

   在编译和调试过程中，出现过 `Serial` 未定义或串口监视器只显示启动日志、不显示程序输出的问题。这与 ESP32-C3 的 USB CDC 串口配置有关。

5. **烧录时开发板未进入下载模式**

   在上传程序时，出现过开发板未进入下载模式的情况，导致程序无法正常烧录。这类问题通常与 ESP32-C3 的 BOOT 状态、复位时机以及外接传感器对启动电平的影响有关。

## 解决方案

1. **重新规划 ESP-C3-32S 引脚**

   根据 ESP-C3-32S 的实际引脚资源重新进行接口分配，将 MQ-2 气敏传感器 AO 接 GPIO0，MH-RD 雨滴传感器 AO 接 GPIO1，FC-28 土壤湿度传感器 AO 接 GPIO2，并同步修改 `config.h` 中的引脚定义，保证软件配置与硬件连接一致。

2. **优先采用 AO 模拟量接口采集**

   本次任务以基础数据采集和串口检测为主，因此优先接入 AO 模拟量接口。DO 数字量接口暂不接入，后续如果需要阈值报警功能，再单独扩展数字量检测逻辑。

3. **严格控制 JSON Payload 字段**

   正式 Payload 严格保留协议规定字段，只输出 `node_id`、`data_type`、`value`、`unit`、`timestamp` 和 `seq`。调试状态信息只作为串口辅助输出，不写入 JSON 消息中，保证数据格式与项目协议保持一致。

4. **调整 PlatformIO 工程配置**

   在 `platformio.ini` 中根据 ESP-C3-32S 的特点调整开发板配置，并加入相关 USB 串口编译选项，使工程能够正常编译。同时，通过观察串口启动日志判断开发板是否已经正常启动，再进一步排查程序串口输出通道问题。

5. **采用手动下载模式进行烧录**

   在程序上传时，通过按住 BOOT 键并配合 RST/EN 键的方式，使 ESP-C3-32S 进入下载模式后再进行烧录。为避免外接传感器影响启动电平，烧录时优先断开传感器信号线，烧录完成后再重新接入。

## 今日小结

今天主要完成了 ESP32 硬件节点中三个真实传感器的基础接线设计和采集程序编写。通过将 MQ-2 气敏传感器、MH-RD 雨滴传感器和 FC-28 土壤湿度传感器分别模块化处理，使程序结构更加清晰，便于后续继续扩展其他传感器。虽然今天还没有连接 WiFi 模块，也没有实现 MQTT 实际发布，但已经按照 MQTT 主题和 JSON 消息格式对采集数据进行了封装，并通过串口输出进行观察和验证，为后续 ESP32 接入 MQTT Broker 并上传真实环境数据奠定了基础。

## 下一步工作计划

下一步将继续完善 ESP32 硬件节点的串口调试工作，重点观察三个传感器在不同环境下的数值变化情况。例如，测试 MQ-2 在不同气味或烟雾环境下的 ADC 值变化，测试雨滴传感器在干燥和滴水状态下的数值变化，测试土壤湿度传感器在空气、干土和湿土中的输出差异。

完成基础采集稳定性验证后，再进一步考虑接入 WiFi 模块获取时间，为 ESP32 接入 MQTT Broker 并上传真实环境数据做准备。后续如果领取到 DHT22、BH1750、声音传感器等其他模块，也将按照当前模块化结构继续增加对应采集代码，并统一接入系统数据上传流程。

**日期：2026年6月11日**  

## 1. 今日主要完成工作内容

今天主要围绕课程设计中期检查反馈问题进行了修改和完善，并参加了课设中期答辩。根据老师在中期检查中提出的意见，对项目工程需求、ESP32 WiFi 配网方式以及传感器模块设计进行了调整。

在硬件节点程序方面，进一步完善了 ESP32 采集节点的程序结构，删除了原有雨量传感器相关内容，并根据现有硬件条件加入 DHT11 温湿度传感器模块。同时，按照预先规划好的 GPIO 接口顺序，对各传感器接口进行了重新整理，使程序结构更加清晰，便于后续扩展和调试。

## 2. 本人完成具体任务

（1）参加课程设计中期答辩，听取老师对项目当前阶段存在问题的反馈意见，明确后续修改方向。

（2）根据中期检查反馈，重新梳理项目工程需求，明确系统需要补充节点数量、试用场景、节点部署方式和实际应用目标等内容，使项目设计更加贴近实际环境监测场景。

（3）对 ESP32 硬件节点的传感器模块进行了调整。由于雨量传感器缺少合适的模拟环境和测试方法，决定删除雨量检测模块及其相关程序内容。

（4）新增 DHT11 温湿度传感器采集模块，实现温度和湿度数据采集，并将其加入现有程序结构中。

（5）按照预先规划好的节点接口重新分配 GPIO 引脚：GPIO0 用于 DHT11 温湿度传感器，GPIO1 预留光照传感器，GPIO2 预留噪声检测模块，GPIO3 用于土壤湿度检测模块，GPIO4 用于气体传感器模块。

（6）调整 MQTT 输出数据顺序，使其按照“温湿度、光照预留、噪声预留、土壤湿度、气体传感器”的顺序进行组织，保证后续数据上传和解析逻辑更加统一。

（7）根据老师建议，开始实现 ESP32 的 WiFi 配网网页门户功能，采用 AP 配网方式，避免在程序中硬编码 WiFi 名称和密码，提高 ESP32 节点在不同网络环境下的适应能力。

（8）将 WiFi 配网功能模块化，新增独立的 WiFi 配网模块，使主程序只需要调用相关接口即可完成 WiFi 初始化、网页配网处理、连接状态判断和配置信息清除等功能。

（9）完善 WiFi 连接状态显示功能，使 ESP32 在首次连接到热点成功后，可以在串口端显示当前连接的 WiFi 基本信息，包括 SSID、IP 地址、网关、子网掩码、DNS、MAC 地址和信号强度等内容，便于后续连接不同手机热点时进行调试。

（10）加入 WiFi 断线重连检测逻辑，当网络连接中断后，ESP32 能够暂停数据采集，并在重新连接成功后统计断网恢复所花费的时间，在串口端以毫秒级显示重连耗时。

（11）新增北京时间同步功能，使 ESP32 在 WiFi 连接成功后通过 NTP 获取北京时间，并将采集数据中的时间戳由本地运行时间改为标准时间格式，例如 `2026-06-08T22:30:00+08:00`。

（12）调整主循环逻辑，使程序只有在 WiFi 连接成功且北京时间同步完成后，才会按照 30 秒的周期采集传感器数据，并在串口端输出可用于后续 MQTT 发送的 Topic 和 JSON Payload。

## 3. 遇到的问题

问题1：项目在中期检查中被指出缺少明确的工程需求，例如节点铺设数量、试用场景和系统实际应用范围等内容不够清楚。

问题2：原程序中 WiFi 名称和密码采用硬编码方式，不利于后续更换网络环境，也不符合物联网设备实际部署需求。

问题3：雨量传感器缺少合适的实验模拟环境，难以通过稳定、可重复的方法完成采集验证。

问题4：加入 WiFi 配网页面后，手机端一开始无法正常打开 ESP32 提供的网页门户。

问题5：WiFi 连接成功后，串口信息较多，如果 WiFi 基本信息反复打印，会影响后续观察传感器采集数据。

问题6：原先时间戳采用本地运行时间，不能准确表示数据的真实采集时间，不利于后续数据记录和分析。

## 4. 解决方法

解决1：根据老师意见重新梳理项目需求，后续将在文档中补充试用场景、节点部署数量、节点功能划分和实际应用目标等内容。

解决2：采用 AP 配网网页门户方式，实现 ESP32 免硬编码 WiFi 账号密码。ESP32 在未连接 WiFi 或连接失败时自动开启热点，用户可通过手机或电脑访问网页填写 WiFi 信息。

解决3：删除雨量传感器模块，避免将难以验证的数据作为当前系统的正式采集内容。改为加入现有 DHT11 温湿度传感器，提高硬件采集功能的可验证性。

解决4：通过调整 ESP32 的 WiFi 工作模式，使用更稳定的 AP 模式启动网页配网门户，并指导手机连接 ESP32 热点后通过 `http://192.168.4.1` 访问配网页面。

解决5：优化 WiFi 信息显示逻辑，使完整的 ESP32 WiFi Basic Info 只在首次连接到新热点成功后打印一次，后续断网重连时只显示重连耗时、SSID 和 IP 地址。

解决6：新增 NTP 时间同步模块，在 WiFi 连接成功后同步北京时间，并将数据时间戳统一为 ISO 8601 格式，便于后续 MQTT 数据上传和数据记录。

## 5. 今日完成结果

通过今天的修改，ESP32 硬件节点程序结构更加清晰，传感器接口分配更加规范。当前系统已经能够完成 WiFi 网页配网、WiFi 状态显示、断线重连耗时统计、北京时间同步以及 WiFi 连接成功后每 30 秒采集一次传感器数据并在串口端输出。

目前串口输出的数据已经包含 MQTT Topic 和 JSON Payload，后续可以在此基础上继续接入 MQTT 发布功能，实现 ESP32 采集数据向上位机或服务器端的无线传输。

## 6. 明日计划

下一步计划继续完善 MQTT 通信部分，将当前串口输出的 Topic 和 JSON Payload 接入 MQTT 发布模块，实现 ESP32 节点通过 WiFi 将采集到的温湿度、土壤湿度和气体传感器数据发送到 MQTT Broker。同时继续完善项目需求文档和中期检查反馈修改内容，为后续系统联调和课程设计报告撰写做准备。

## 日期：2026年6月12日

## 今日主要工作内容

今天主要围绕 ESP32 硬件节点的数据联网传输功能进行完善和测试调试。在前期已经完成 DHT11 温湿度传感器、MQ-2 气体传感器、FC-28 土壤湿度传感器基础采集和串口输出的基础上，进一步根据组内联调规范，对 MQTT 主题格式、Payload 数据格式、时间戳格式、数据类型命名、发布顺序和网络连接流程进行了统一调整。

本次工作的重点是将 ESP32 节点的数据输出从单纯的串口调试阶段，逐步过渡到 MQTT 网络传输阶段。为保证后续可以与电脑端 MQTT Broker、MQTTX 或监控页面正常联调，需要先确认 ESP32 能够连接笔记本电脑开启的 Windows 移动热点，并按照规定的数据格式生成 MQTT 消息。

根据组内联调规范，ESP32 硬件节点需要连接 Windows 移动热点，MQTT Broker 地址固定为 `192.168.137.1:1883`，连接方式采用匿名连接。MQTT 主题统一为 `env_monitor/{node_id}/{data_type}`，其中 `node_id` 使用 `node03`，数据类型包括 `temperature`、`humidity`、`smoke` 和 `soil_moisture`。Payload 需要使用紧凑 JSON 格式，时间戳使用 NTP 对时后的 epoch 毫秒整数，序号使用节点内全局递增序号。

## 本人完成的具体任务

1. 重新统一 ESP32 硬件节点的数据类型命名，将 DHT11 温度数据设置为 `temperature`，DHT11 湿度数据设置为 `humidity`，MQ-2 气体传感器原始 ADC 数据设置为 `smoke`，FC-28 土壤湿度百分比数据设置为 `soil_moisture`。

2. 修改传感器采集模块，使每个传感器采集结果能够按照统一的数据结构保存，便于后续 MQTT 消息封装和网络发布。

3. 调整 MQTT Payload 格式，将原来较完整的 JSON 输出改为更适合网络传输和电脑端解析的紧凑 JSON 格式，例如：

   `{"n":"node03","d":"temperature","v":26.31,"t":<epoch毫秒>,"s":<序号>}`

4. 将时间戳格式由原来的北京时间字符串调整为 NTP 对时后的 epoch 毫秒整数，使电脑端后续存储数据时能够更方便地进行排序、筛选和统计。

5. 按照 QoS 1 发布要求，尝试使用 `256dpi/arduino-mqtt` 库实现 MQTT 发布功能，并对 PlatformIO 工程中的库依赖配置进行了检查和修改。

6. 在使用 `256dpi/arduino-mqtt` 库时，工程中出现 `MQTTClient` 未定义等问题。为保证工程继续推进，改用 `AsyncMqttClient` 库实现 MQTT 客户端功能。

7. 完成 MQTT 初始化、Broker 连接、断线重连、QoS 1 发布和发布确认回调等功能，使 ESP32 在连接 Broker 后能够按照固定周期发布传感器数据。

8. 修改 `mqtt_client.h` 和 `mqtt_client.cpp`，将 MQTT 连接、重连、发布和发布确认逻辑独立封装，减少主程序中的代码耦合。

9. 修改 `main.cpp`，将主程序运行流程调整为：WiFi 连接成功后进行 NTP 对时，时间同步成功后连接 MQTT Broker，MQTT 连接成功后每隔 30 秒采集并发布一次数据。

10. 对 ESP32 连接 Windows 移动热点的流程进行了测试，明确需要先通过 ESP32 自建热点 `ESP32C3_Config` 进入配网页面，再填写 Windows 移动热点名称和密码，使 ESP32 保存网络信息后连接电脑热点。

11. 对串口输出信息进行了多次检查，重点观察 WiFi 状态、IP 地址、网关、NTP 时间同步结果、MQTT Topic、Payload 字段和发布顺序是否符合联调要求。

## 遇到的问题

1. 使用 `256dpi/arduino-mqtt` 库时，PlatformIO 工程中出现 `MQTTClient` 未定义问题，导致 MQTT 客户端模块无法正常编译。

2. 原有串口输出 JSON 格式与组内 MQTT 联调规范不一致，主要体现在 Topic 格式、Payload 字段、时间戳格式和数据类型命名不统一。

3. ESP32 AP 配网页面在手机端访问不稳定，存在页面打不开、填写 Windows 热点账号和密码后页面丢失等现象。

4. 手机连接 ESP32 自建热点后，由于 `ESP32C3_Config` 本身不能访问互联网，手机可能自动切换到移动数据或其他 WiFi，导致无法继续访问 `http://192.168.4.1`。

5. MQTT 发布前需要同时满足 WiFi 连接、NTP 时间同步和 MQTT Broker 连接三个条件，任意一个环节异常都会导致数据无法正常发布，因此调试时需要逐步排查每个环节。

## 解决方法

1. 针对 `MQTTClient` 无法识别的问题，先检查 PlatformIO 的库依赖配置和工程编译情况。由于该问题持续影响后续联调进度，最终改用支持 QoS 1 发布的 `AsyncMqttClient` 库完成 MQTT 发布功能。

2. 根据组内联调规范重新设计 MQTT 消息格式，将主题统一为 `env_monitor/{node_id}/{data_type}`，将 Payload 统一为紧凑 JSON，并将时间戳统一改为 NTP 对时后的 epoch 毫秒整数。

3. 对 AP 配网页面不稳定问题进行了记录和分析，初步判断该问题与手写 captive portal 的 DNS 劫持、页面跳转处理以及手机网络切换机制有关，并将其整理为 GitHub Issue，作为后续优化方向。

4. 在测试说明中明确：连接 `ESP32C3_Config` 后如果没有自动弹出配网页，需要手动访问 `http://192.168.4.1`；调试时可以临时关闭手机移动数据，避免手机自动切换网络。

5. 在程序中增加更多串口提示信息，通过串口分阶段观察 WiFi、NTP、MQTT 和传感器采集状态，避免多个模块同时出错时难以定位问题。

## 今日成果

通过今天的工作，ESP32 硬件节点的数据联网传输框架基本形成。当前程序已经包含传感器采集模块、WiFi 配网模块、NTP 时间同步模块、MQTT 消息封装模块和 MQTT 客户端发布模块。

程序能够按照 `env_monitor/{node_id}/{data_type}` 的主题格式组织数据，并使用紧凑 JSON 格式封装 Payload。主程序运行逻辑已经调整为：WiFi 连接成功后进行 NTP 时间同步，时间同步成功后连接 MQTT Broker，MQTT 连接成功后每隔 30 秒采集并发布一次环境数据。

在测试调试方面，今天重点完成了 ESP32 连接 Windows 移动热点的流程验证、串口 WiFi 信息观察、NTP 时间同步检查、MQTT 消息格式检查和发布前逻辑检查。通过串口输出可以较清楚地判断 ESP32 当前处于 WiFi 未连接、时间未同步、MQTT 未连接或数据发布状态，为后续实际联调提供了较好的调试基础。

同时，针对 AP 配网页面不稳定的问题，已经形成明确的问题记录和后续优化方向，为下一步改用成熟配网库、提高配网稳定性打下基础。

## 明日计划

明天计划继续优化 ESP32 的 WiFi 配网功能，重点解决 AP 配网页面访问不稳定的问题。计划尝试使用成熟的 WiFiManager 配网库替换当前手写配网逻辑，提高 ESP32 配网门户在手机和电脑端访问时的稳定性。

同时，继续进行 ESP32 与 Windows 移动热点、电脑端 MQTT Broker 之间的联调测试。后续需要使用 MQTTX 或监控页面订阅 `env_monitor/+/+`，检查是否能够稳定接收到 `temperature`、`humidity`、`smoke` 和 `soil_moisture` 四类数据，并继续观察串口中 WiFi、NTP 和 MQTT 状态是否稳定。

---

## 日期：2026年6月13日


## 今日主要工作内容

今天主要围绕 ESP32 硬件节点的 WiFi 配网稳定性和热点切换功能进行优化和测试。针对前一天测试中发现的手写 AP 配网页面不稳定问题，决定使用成熟的 `tzapu/WiFiManager` 库替换原先手写的 `wifi_config` 配网逻辑，减少手写 WebServer、DNSServer、Preferences 和表单处理带来的不稳定因素。

本次工作的重点不仅是替换配网库，还需要保证替换后原有主程序流程不被破坏，即 ESP32 在完成 WiFi 配网后仍然能够继续执行 NTP 时间同步、MQTT Broker 连接和传感器数据发布流程。同时，还需要解决一个实际测试中出现的问题：ESP32 已经连接电脑热点后，如果关闭电脑热点，设备会持续尝试重连旧热点，手机无法重新发现 `ESP32C3_Config` 配网热点，导致无法更换新的热点。

因此，今天重点完善了断线重连和重新进入配网模式的逻辑，使 ESP32 在旧热点不可用时，可以先尝试重连；如果长时间无法恢复连接，则自动重新开启 `ESP32C3_Config` 配网门户，便于用户重新配置其他热点。

## 本人完成的具体任务

1. 分析原有手写 captive portal 配网页面不稳定的原因，主要包括 DNS 劫持不完善、手机无法稳定自动弹出配网页、热点无网络时手机自动切换移动数据、网页提交后重启时机不合理等。

2. 修改 `platformio.ini` 文件，新增 `tzapu/WiFiManager` 库依赖，使工程能够使用 WiFiManager 提供的自动连接和 AP 配网功能。

3. 重写 `wifi_config.cpp`，使用 WiFiManager 的 `autoConnect()` 替换原有手写 AP 配网逻辑，实现“已保存 WiFi 自动连接，未保存或连接失败时自动开启 AP 配网热点”的功能。

4. 保留原有接口函数，包括 `initWiFiConfig()`、`handleWiFiConfig()`、`isWiFiConnected()`、`getWiFiStatusText()`、`clearSavedWiFiConfig()` 和 `printWiFiBasicInfo()`，避免 `main.cpp` 和其他模块发生大范围改动。

5. 修改并检查 `main.cpp`，使程序启动后先执行 WiFiManager 配网流程。若已有保存的热点信息，则自动连接；若没有保存信息或连接失败，则开启 `ESP32C3_Config` 热点进入配网模式。

6. 在 `wifi_config.cpp` 中增加 WiFi 状态监测变量，用于记录上一次连接状态、断线开始时间和上一次重连尝试时间。

7. 完善 `handleWiFiConfig()` 断线重连逻辑。ESP32 检测到 WiFi 断开后，先按设定时间间隔尝试重连旧热点；如果超过设定时间仍未连接成功，则自动启动 WiFiManager 配网门户，重新生成 `ESP32C3_Config` 热点。

8. 增加断线重连耗时统计功能。当 ESP32 断网后重新连接成功时，通过串口输出重连耗时、当前 SSID 和当前 IP 地址，便于判断网络恢复情况。

9. 对 WiFiManager 默认配网页面进行了确认。进入 `http://192.168.4.1` 后，页面显示 `Configure WiFi`、`Info`、`Exit`、`Erase`、`Restart` 等选项，确认这是 WiFiManager 默认页面，不是程序异常。

10. 对 WiFiManager 页面操作流程进行了验证，确认用户需要点击 `Configure WiFi`，选择 Windows 移动热点或其他热点，输入密码后保存，ESP32 才会连接对应网络。

11. 对旧热点关闭后的重新配网流程进行了修改和测试，使 ESP32 在旧热点不可用时能够重新开启配网热点，解决了更换热点时无法发现 ESP32 WiFi 的问题。

## 遇到的问题

1. 原先手写 AP 配网页面在手机端访问不稳定，影响 ESP32 连接 Windows 移动热点和后续 MQTT 联调。

2. ESP32 连接旧热点后，如果关闭该热点，设备会持续尝试重连旧网络，无法自动重新出现 `ESP32C3_Config` 配网热点，导致用户无法重新进入配网页面更换热点。

3. 改用 WiFiManager 后，进入 `http://192.168.4.1` 的页面样式与原先手写页面不同，容易误认为代码运行异常。

4. 手机连接 ESP32 配网热点后，部分情况下仍可能因为热点无网络而切换到移动数据，导致页面访问失败。

5. WiFiManager 的 `startConfigPortal()` 进入配网门户时具有阻塞特性，在用户未完成配网或配网页面未超时前，主程序后续的 NTP、MQTT 和采集流程会暂停，需要在逻辑设计中明确这一点。

## 解决方法

1. 使用 `tzapu/WiFiManager` 库替换手写 AP 配网逻辑，利用其内置的自动连接、AP 配网、DNS 劫持、网页配置和 WiFi 信息保存功能，提高配网稳定性。

2. 在 `handleWiFiConfig()` 中加入重连超时机制。WiFi 断开后先尝试重连旧热点，若超时仍未成功，则自动启动 WiFiManager 配网门户，重新开启 `ESP32C3_Config` 热点，方便配置新的网络。

3. 对 WiFiManager 页面变化进行确认和说明，明确当前页面是库自带默认页面，后续通过 `Configure WiFi` 选择热点并输入密码即可完成配网。

4. 在调试过程中采用关闭手机移动数据、或使用电脑连接 `ESP32C3_Config` 的方式进行配网，减少手机自动切换网络对页面访问的影响。

5. 在串口中增加 WiFi 状态提示、断线提示、重连尝试提示、重新进入配网提示和 WiFi 基本信息输出，使调试过程中可以清楚判断 ESP32 当前处于正常连接、断线重连还是重新配网状态。

6. 保留 `clearSavedWiFiConfig()` 函数，用于在需要主动更换热点或清除错误热点信息时临时调用，避免 Flash 中保存的旧网络信息影响配网测试。

## 今日成果

通过今天的修改，ESP32 的 WiFi 配网模块稳定性明显提升。当前程序已经能够实现已保存 WiFi 自动连接、无可用网络时开启 AP 配网热点、保存新热点信息、旧热点断开后尝试重连以及重连失败后重新进入配网模式等功能。

在测试调试方面，今天完成了 WiFiManager 库依赖编译测试、ESP32 配网热点生成测试、`http://192.168.4.1` 页面访问测试、WiFiManager 默认页面确认、Windows 移动热点连接测试、串口 WiFi 基本信息检查、关闭电脑热点后的断线测试、旧热点重连测试和重连失败后重新开启配网门户测试。

测试过程中确认：当 ESP32 没有保存 WiFi 或旧热点无法连接时，可以生成 `ESP32C3_Config` 配网热点；手机或电脑连接该热点后，可以通过 `http://192.168.4.1` 进入 WiFiManager 页面；点击 `Configure WiFi` 后可以选择新的热点并填写密码；ESP32 连接成功后，串口能够输出 SSID、IP 地址、网关、子网掩码、DNS、MAC 地址和 RSSI 等信息。

同时，通过关闭 Windows 移动热点的方式验证了断线处理逻辑。ESP32 检测到网络断开后，会先尝试重连旧热点；如果超过设定时间仍未连接成功，则重新开启 `ESP32C3_Config` 配网门户，使用户可以重新配置其他热点。该功能解决了更换热点时无法发现 ESP32 WiFi 的问题。

本次修改后，原有 NTP 时间同步、MQTT Broker 连接和传感器数据发布流程仍然保留。ESP32 成功连接 Windows 移动热点后，仍可继续按照项目规范连接 `192.168.137.1:1883`，并向 `env_monitor/{node_id}/{data_type}` 主题发布数据。

## 明日计划

明天计划继续进行完整联调测试。首先确认 ESP32 能够稳定通过 WiFiManager 连接 Windows 移动热点，并在串口中稳定显示 `192.168.137.xxx` 网段 IP 和 `192.168.137.1` 网关。

随后启动电脑端 MQTT Broker，使用 MQTTX 或监控页面订阅 `env_monitor/+/+`，检查是否能够稳定接收到 `temperature`、`humidity`、`smoke` 和 `soil_moisture` 四类数据。同时继续观察 QoS 1 发布确认、断线重连、热点切换和长时间运行情况下的稳定性，为后续 24 小时连续采集和数据保存测试打好基础。
```
