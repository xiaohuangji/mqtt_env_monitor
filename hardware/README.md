# 硬件节点说明

本目录用于保存 ESP32 环境监测节点相关内容。

## 目录说明

- `esp32_nodes/`：存放 ESP32 节点程序。后续每个节点可按传感器类型或节点编号建立独立目录。
- `wiring/`：存放接线图、传感器说明、引脚分配和硬件调试记录。

## 后续硬件目标

- 使用 ESP32 连接 Wi-Fi。
- 读取温湿度、光照或噪声等环境数据。
- 使用 MQTT 将数据发布到 Broker。
- 支持设置上传周期。
- 支持断线重连和串口日志输出。

