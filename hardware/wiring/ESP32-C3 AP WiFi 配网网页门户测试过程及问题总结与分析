# ESP32-C3 AP WiFi 配网网页门户测试问题总结与分析

## 1. 测试背景

本次测试采用 ESP32-C3 的 AP WiFi 配网网页门户方案。正常流程为：手机或电脑先连接 ESP32 创建的临时热点 `ESP32C3_Config`，然后在浏览器中访问 `http://192.168.4.1`，进入配网页面后填写 Windows 移动热点名称和密码。ESP32 接收到配置信息后保存 WiFi 信息并重启，随后自动连接 Windows 移动热点，为后续 NTP 时间同步、传感器数据上传和 MQTT 联调提供网络基础。

经过多次调试后，ESP32-C3 最终能够成功连接目标 WiFi，并在串口中输出连接成功信息、IP 地址和后续传感器数据，说明 AP 配网流程最终调试成功。

## 2. 测试过程中出现的问题

在实际测试过程中，主要出现了以下问题：

1. 手机浏览器访问 `http://192.168.4.1` 时，网页经常无法正常打开；
2. 即使偶尔进入配网页面，在填写 Windows 热点名称和密码后，页面会直接丢失或无响应；
3. 无法稳定确认 ESP32 是否已经成功保存热点信息并重启；
4. 该问题影响 ESP32 后续连接 Windows 移动热点，也影响 MQTT 联调流程。

## 3. 问题原因分析

首先，ESP32 创建的 `ESP32C3_Config` 热点属于临时 AP 配网热点，通常不能访问互联网。部分手机在连接这种热点后，会提示“当前 WLAN 无法访问互联网”，并可能自动切换到移动数据、校园网或其他已保存 WiFi。此时用户虽然以为手机仍然连接在 ESP32 热点上，但浏览器请求已经不再发送给 ESP32，因此访问 `http://192.168.4.1` 时会出现网页无法打开、加载失败或无响应的情况。

其次，浏览器访问方式也可能造成问题。ESP32 WebServer 一般只提供普通 HTTP 服务，不支持 HTTPS。如果浏览器自动将地址补全为 `https://192.168.4.1`，页面就会访问失败。此外，浏览器缓存、自动跳转或网络检测页面也可能影响配网页面的正常显示。

第三，填写 WiFi 信息并提交后，ESP32 可能会立即保存配置并重启。设备一旦重启，原本由 ESP32 创建的 `ESP32C3_Config` 热点会短暂消失，手机与 ESP32 的连接随之断开，所以网页会表现为丢失、空白或无响应。这种情况不一定代表程序错误，也可能是 ESP32 正在正常保存配置并重启。

第四，ESP32 从 AP 模式切换到 STA 模式后，原来的 `192.168.4.1` 地址会失效。配网阶段的 `192.168.4.1` 是 ESP32 在 AP 模式下的默认地址；当 ESP32 连接到 Windows 移动热点后，会由 Windows 热点重新分配一个新的局域网 IP 地址。因此提交配置后继续访问 `192.168.4.1`，可能无法再打开原页面。

第五，Windows 移动热点本身可能存在不稳定因素。Windows 热点受电脑无线网卡、系统网络共享、防火墙、省电策略等影响，稳定性通常不如普通路由器。如果热点名称或密码输入错误、热点临时关闭、防火墙限制后续端口访问，都会影响 ESP32 连接和后续 MQTT 联调。

## 4. 校园网覆盖环境的影响分析

在测试过程中，即使手机没有主动连接校园网，校园网覆盖环境仍然可能间接影响 ESP32 AP 配网页面稳定性。需要注意的是，访问 `http://192.168.4.1` 理论上只依赖 ESP32 自身创建的局部热点，不需要校园网参与。只要手机稳定连接在 `ESP32C3_Config` 上，就应该能够正常打开配网页面。

但是在校园网覆盖较强的环境下，手机可能会因为 ESP32 热点无法访问互联网，而自动切换到校园网、移动数据或其他已保存网络。此时浏览器访问 `192.168.4.1` 的请求就不会再发送给 ESP32，导致页面丢失或无响应。

同时，校园网中通常存在较多 2.4GHz 无线信号，而 ESP32-C3 也工作在 2.4GHz WiFi 频段。如果周围无线信号较多、信道拥挤，可能会对 ESP32 临时 AP 热点造成一定干扰，表现为网页加载较慢、提交后卡住或手机连接不稳定。该影响属于无线环境干扰，并不是校园网主动阻止 ESP32 配网。

## 5. 解决方法

针对网页打不开的问题，测试时应确保手机真正连接到 `ESP32C3_Config` 热点。如果手机提示“该网络无法访问互联网”，应选择“仍然连接”或“保持连接”。必要时可以暂时关闭手机移动数据，并关闭校园网自动连接，避免手机自动切换网络。

访问配网页面时，应手动输入 `http://192.168.4.1`，注意必须使用 `http://`，不要使用 `https://`。如果页面仍然无法打开，可以更换浏览器、使用无痕模式，或等待串口显示 AP 和 WebServer 启动完成后再访问。

针对提交后页面丢失的问题，可以在程序中优化网页反馈逻辑。ESP32 接收到 WiFi 信息后，应先返回一个提示页面，例如“WiFi 信息已提交，设备即将保存并重启”，再延时执行重启操作。这样即使页面随后断开，用户也能知道这是正常的配网流程。

针对无法确认是否保存成功的问题，应增加清晰的串口日志输出。程序可以依次打印“进入配网模式”“收到 WiFi 信息”“正在保存配置”“保存成功”“准备重启”“正在连接已保存 WiFi”“连接成功”“当前 IP 地址”等信息。调试时应以串口日志作为判断 ESP32 是否成功保存和连接的主要依据。

针对 Windows 热点和 MQTT 联调问题，应保证 Windows 移动热点处于开启状态，并尽量使用简单的英文数字热点名称和密码，避免特殊字符。ESP32 连接成功后，应查看串口输出的 IP 地址、网关和 RSSI 信号强度，确认设备已经接入 Windows 热点。如果后续进行 MQTT 联调，还需要保证电脑端 MQTT Broker 正常运行，并在 Windows 防火墙中允许 MQTT 端口通信，例如 1883 端口。

## 6. 最终调试结果

经过多次调试后，通过保持手机稳定连接 `ESP32C3_Config` 热点、手动访问 `http://192.168.4.1`、正确填写 Windows 热点名称和密码，并结合串口日志观察 ESP32 状态，最终成功完成 AP WiFi 配网。ESP32-C3 能够保存目标 WiFi 信息，重启后自动连接 Windows 移动热点，并在串口中输出连接成功信息和 IP 地址。

最终结果表明，ESP32-C3 的 AP WiFi 配网网页门户方案能够正常使用。前期出现的网页丢失或无响应问题，主要与手机自动切换网络、ESP32 重启导致 AP 热点断开、浏览器访问方式、校园网覆盖环境以及 Windows 热点稳定性有关。通过规范操作流程、增强串口提示、优化网页反馈和保持测试网络稳定，可以有效提高配网成功率，为后续 MQTT 联调和传感器数据上传提供网络基础。
