# ESP32-C3-32S 传感器模块功能与接口说明

## 1. 说明

本文档用于说明 ESP32-C3-32S 环境监测硬件节点中已经完成接入的传感器模块功能，以及各模块 VCC、GND、DATA、AO、DO 等接口的具体含义。文档内容可用于后续接线检查、程序编写、调试记录和课程设计报告整理。

当前硬件节点以 ESP32-C3-32S 为核心控制单元，已经完成以下四个模块的接入与说明：

1. DHT11 温湿度传感器模块；
2. 光照传感器模块；
3. FC-28 土壤湿度传感器模块；
4. MQ-2 气体传感器模块。

本次修改删除噪声检测模块相关内容，不再将其作为当前项目模块或预留模块说明。当前文档只保留已经作为项目完成内容的四个传感器模块。

---

## 2. 当前传感器模块总体说明

| 模块名称 | 数据类型 | 功能说明 | 当前状态 |
| --- | --- | --- | --- |
| DHT11 温湿度传感器 | `temperature` / `humidity` | 用于采集环境温度和空气相对湿度 | 已接入 |
| 光照传感器 | `light` | 用于采集环境光照强度或光照变化情况 | 已接入 |
| FC-28 土壤湿度传感器 | `soil_moisture` | 用于检测土壤含水量变化 | 已接入 |
| MQ-2 气体传感器 | `gas` | 用于检测烟雾、可燃气体等气体浓度变化 | 已接入 |

四个模块覆盖了环境监测节点中较常见的温度、湿度、光照、土壤湿度和气体检测数据。其中 DHT11 一个模块可以同时输出温度和湿度两类数据，因此当前硬件上接入四个模块，实际可形成五类环境数据。

---

## 3. ESP32-C3-32S 引脚分配说明

| GPIO 引脚 | 模块名称 | 接口类型 | 数据类型 | 当前用途 |
| --- | --- | --- | --- | --- |
| GPIO0 | DHT11 温湿度传感器 | DATA | `temperature` / `humidity` | 当前接入 |
| GPIO1 | 光照传感器 | AO | `light` | 当前接入 |
| GPIO3 | FC-28 土壤湿度传感器 | AO | `soil_moisture` | 当前接入 |
| GPIO4 | MQ-2 气体传感器 | AO | `gas` | 当前接入 |

该引脚分配方案符合当前项目的实际硬件接入情况。GPIO0 用于数字温湿度数据读取，GPIO1、GPIO3、GPIO4 分别用于模拟量传感器的 ADC 数据采集。

---

## 4. 通用接口功能说明

不同传感器模块的接口形式略有差异，但常见接口包括 VCC、GND、DATA、AO 和 DO。

| 接口 | 名称 | 功能说明 |
| --- | --- | --- |
| VCC | 电源正极 | 给传感器模块供电，本项目优先接 ESP32-C3-32S 的 3.3V |
| GND | 电源地 | 接 ESP32-C3-32S 的 GND，保证传感器与主控板共地 |
| DATA | 数字数据接口 | 用于 DHT11 等数字传感器的数据通信 |
| AO | Analog Output，模拟输出 | 输出随环境变化而变化的模拟电压，接 ESP32-C3 的 ADC 引脚 |
| DO | Digital Output，数字输出 | 由模块比较器和电位器阈值决定，输出高/低电平 |

### 4.1 VCC 接口说明

VCC 是传感器模块的电源输入端。本项目中，为了避免传感器输出电压超过 ESP32-C3 的输入范围，优先将传感器模块 VCC 接到 ESP32-C3-32S 的 3.3V。若部分模块需要 5V 供电，应确认其信号输出是否会超过 3.3V，必要时需要增加电平转换或分压保护。

### 4.2 GND 接口说明

GND 是传感器模块的地线接口。所有传感器模块必须与 ESP32-C3-32S 共地，否则 ESP32-C3 读取到的数字信号或模拟电压值可能不稳定，甚至出现无法正常读取的情况。

### 4.3 DATA 接口说明

DATA 是数字数据接口，常见于 DHT11、DHT22 等数字温湿度传感器。DHT11 通过 DATA 引脚按照特定时序输出温度和湿度数据，ESP32-C3 需要通过对应库函数或时序程序进行读取。

### 4.4 AO 接口说明

AO 是模拟输出接口，会输出一个与被测环境量相关的模拟电压。例如光照传感器的 AO 会随光照强弱变化，土壤湿度模块的 AO 会随土壤干湿程度变化，MQ-2 的 AO 会随气体浓度变化。ESP32-C3 通过 ADC 功能读取 AO 电压，并将其转换为 ADC 原始值或进一步换算后的环境参数。

### 4.5 DO 接口说明

DO 是数字输出接口，通常由模块上的 LM393 比较器和电位器控制。当传感器检测值超过设定阈值时，DO 会输出高电平或低电平。DO 适合做简单报警或阈值触发，但不能反映连续变化趋势。本项目当前主要进行数据采集和趋势观察，因此光照传感器、土壤湿度模块和 MQ-2 模块优先使用 AO，DO 暂不接入。

---

## 5. DHT11 温湿度传感器模块详细说明

### 5.1 模块功能

DHT11 是常见的数字式温湿度传感器，可用于采集环境温度和空气相对湿度。本项目中，DHT11 作为 ESP32-C3 硬件节点的温湿度采集模块，对应输出两类数据：

- `temperature`：环境温度；
- `humidity`：空气相对湿度。

DHT11 的优势是接线简单、输出为数字信号、程序读取方便，适合课程设计和基础环境监测场景。

### 5.2 接口功能

| DHT11 引脚 | 功能说明 | ESP32-C3-32S 连接 |
| --- | --- | --- |
| VCC | 模块供电 | 3.3V |
| GND | 模块地线 | GND |
| DATA | 温湿度数字数据输出 | GPIO0 |

### 5.3 采集方式说明

DHT11 不通过 ADC 读取模拟电压，而是通过 DATA 引脚进行数字通信。ESP32-C3 在程序中通过 DHT11 驱动库或指定时序读取温度和湿度值。读取成功后，可分别生成温度 JSON 数据和湿度 JSON 数据。

示例 Topic：

```text
env_monitor/node03/temperature
env_monitor/node03/humidity
```

### 5.4 使用注意事项

1. DHT11 的 DATA 引脚需要连接到指定 GPIO0；
2. 程序中需要配置 DHT11 的数据引脚和传感器类型；
3. DHT11 采样频率不宜过高，建议间隔一定时间读取；
4. 若读取失败，应检查 VCC、GND、DATA 是否连接正确。

---

## 6. 光照传感器模块详细说明

### 6.1 模块功能

光照传感器用于检测环境光照强度或光照变化情况。本项目中，光照传感器作为环境监测节点的重要组成部分，对应数据类型为：

```text
light
```

光照数据可用于判断环境明暗变化，例如室内光照强度、白天夜晚变化、遮挡检测等。在环境监测系统中，光照数据可以与温湿度、土壤湿度、气体检测数据共同反映当前环境状态。

### 6.2 接口功能

如果当前使用的是模拟光照传感器模块，其常见接口如下：

| 光照传感器引脚 | 功能说明 | ESP32-C3-32S 连接 |
| --- | --- | --- |
| VCC | 模块供电 | 3.3V |
| GND | 模块地线 | GND |
| AO | 光照强度相关的模拟电压输出 | GPIO1 |
| DO | 光照强度达到阈值后的数字输出 | 暂不接 |

### 6.3 采集方式说明

本项目中光照传感器使用 AO 模拟输出接入 GPIO1，ESP32-C3 通过 ADC 读取其原始值。该值可直接作为 `raw` 数据输出，也可以根据实际测试结果进一步换算为光照强度或光照百分比。

示例 Topic：

```text
env_monitor/node03/light
```

示例 Payload 中的单位可设置为：

```text
raw
```

如果后续使用标准光照模块并完成标定，也可以将单位改为 `lux`。

### 6.4 使用注意事项

1. 光照传感器应避免被遮挡或受到无关强光干扰；
2. AO 输出值与光照强弱的关系需要根据实际模块进行测试；
3. 若使用 DO 接口，只能判断是否超过设定阈值，无法反映连续变化；
4. 当前阶段建议使用 AO 进行连续数据采集，便于观察光照变化趋势。

---

## 7. FC-28 土壤湿度传感器模块详细说明

### 7.1 模块功能

FC-28 土壤湿度传感器用于检测土壤含水量变化，常用于植物养护、农业监测和环境监测场景。本项目中，FC-28 对应数据类型为：

```text
soil_moisture
```

该模块通常由双针探头和 LM393 主控板组成。双针探头插入土壤后，土壤含水量变化会影响模块输出信号。ESP32-C3 通过读取 AO 模拟输出值，可以判断土壤干湿程度变化。

### 7.2 接口功能

| FC-28 引脚 | 功能说明 | ESP32-C3-32S 连接 |
| --- | --- | --- |
| VCC | 模块供电 | 3.3V |
| GND | 模块地线 | GND |
| AO | 土壤湿度相关的模拟电压输出 | GPIO3 |
| DO | 达到设定阈值后输出数字电平 | 暂不接 |

### 7.3 采集方式说明

本项目中 FC-28 使用 AO 模拟输出接入 GPIO3，ESP32-C3 通过 ADC 读取其原始值。该原始值可直接作为 `raw` 数据输出，也可以通过干湿标定转换为归一化湿度指标。

土壤湿度标定思路如下：

1. 在空气干燥条件下采集 ADC 值，作为干燥参考值；
2. 将探头置于水中或高湿度条件下，采集多次 ADC 值并取平均，作为湿润参考值；
3. 根据当前 ADC 值在干燥值和湿润值之间的位置，计算土壤湿度百分比或湿度指数。

### 7.4 使用注意事项

1. 传感器探头应稳定插入待测介质中；
2. 读取 ADC 时可进行多次采样取平均，降低波动；
3. 不同土壤类型、插入深度和水分分布会影响测量结果；
4. 课程设计阶段可先输出 ADC 原始值，后续根据实测数据进行标定换算。

---

## 8. MQ-2 气体传感器模块详细说明

### 8.1 模块功能

MQ-2 气体传感器模块可用于检测烟雾、液化气、甲烷、可燃气体等气体浓度变化。本项目中，MQ-2 对应数据类型为：

```text
gas
```

MQ-2 适合用于观察环境中气体浓度变化趋势。由于本项目未进行标准气体浓度标定，因此当前输出值主要作为 ADC 原始值或趋势参考，不直接表示 ppm 浓度。

### 8.2 接口功能

| MQ-2 引脚 | 功能说明 | ESP32-C3-32S 连接 |
| --- | --- | --- |
| VCC | 模块供电 | 3.3V |
| GND | 模块地线 | GND |
| AO | 气体浓度相关的模拟电压输出 | GPIO4 |
| DO | 气体浓度超过设定阈值后输出数字电平 | 暂不接 |

### 8.3 采集方式说明

本项目中 MQ-2 使用 AO 模拟输出接入 GPIO4，ESP32-C3 通过 ADC 读取其原始模拟值。程序将采集到的 ADC 值封装为 `gas` 类型数据，并通过串口输出 Topic 和 JSON Payload。

示例 Topic：

```text
env_monitor/node03/gas
```

示例 Payload 中的单位可设置为：

```text
raw
```

表示当前数据为 ADC 原始值。

### 8.4 使用注意事项

1. MQ-2 模块上电后通常需要一定预热时间，刚启动时数据可能不稳定；
2. 当前未进行标准气体标定，输出值只用于趋势观察；
3. 使用 AO 可观察连续变化，使用 DO 只能判断是否超过阈值；
4. 模块供电和输出电压需要与 ESP32-C3 输入范围匹配，优先使用 3.3V 供电。

---

## 9. 当前模块与 MQTT 数据类型对应关系

| 模块名称 | ESP32-C3 引脚 | 输出数据类型 | Topic 示例 | 单位建议 |
| --- | --- | --- | --- | --- |
| DHT11 温湿度传感器 | GPIO0 | `temperature` | `env_monitor/node03/temperature` | `C` |
| DHT11 温湿度传感器 | GPIO0 | `humidity` | `env_monitor/node03/humidity` | `%RH` |
| 光照传感器 | GPIO1 | `light` | `env_monitor/node03/light` | `lux` 或 `raw` |
| FC-28 土壤湿度传感器 | GPIO3 | `soil_moisture` | `env_monitor/node03/soil_moisture` | `%` 或 `raw` |
| MQ-2 气体传感器 | GPIO4 | `gas` | `env_monitor/node03/gas` | `raw` |

---

## 10. 小结

本阶段 ESP32-C3-32S 硬件节点当前已完成 DHT11 温湿度传感器、光照传感器、FC-28 土壤湿度传感器和 MQ-2 气体传感器的接入说明。当前不再保留噪声检测模块内容，也不再将其作为预留模块说明。

DHT11 使用 DATA 数字接口读取温度和湿度数据；光照传感器、FC-28 和 MQ-2 使用 AO 模拟输出接口，由 ESP32-C3 通过 ADC 读取原始值。DO 数字输出接口当前暂不接入，后续如需实现阈值报警，可进一步扩展使用。

通过上述模块功能和接口说明，可以为后续接线检查、程序模块化编写、串口调试输出和 MQTT 数据发布提供清晰依据。
