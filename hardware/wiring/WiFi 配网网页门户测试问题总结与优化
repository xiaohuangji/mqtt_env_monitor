# ESP32-C3 AP WiFi 配网网页门户测试问题总结与优化

## 一、测试背景

本次测试主要围绕 ESP32-C3 的 AP WiFi 配网网页门户功能展开。该方案的目标是避免在程序中硬编码 WiFi 名称和密码，使 ESP32-C3 能够先开启临时 AP 热点，用户通过手机连接该热点后访问配网页面，再完成目标 WiFi 的配置。

原设计流程为：手机连接 ESP32-C3 创建的配网热点，浏览器访问 `http://192.168.4.1`，在网页中手动输入 Windows 移动热点名称和密码。ESP32-C3 接收信息后保存配置并重启，随后自动连接目标热点，为后续 NTP 时间同步、MQTT 联调和传感器数据上传提供网络基础。

在实际测试过程中，配网页面访问和 WiFi 信息提交存在不稳定情况。经过多次调试后，最终成功连接目标热点，并进一步对配网方式进行了优化：将原本需要手动输入 WiFi 名称的方式，改为 ESP32 自动扫描附近 WiFi 账号并在网页中显示列表，用户可以直接选择目标热点，再输入密码进行连接。

## 二、测试过程中出现的问题

### 1. 配网页面无法稳定打开

手机连接 ESP32-C3 创建的 AP 配网热点后，访问 `http://192.168.4.1` 时，网页经常无法正常打开，表现为页面长时间加载、提示无法访问或需要多次刷新才能进入配网页面。

该问题可能是因为 ESP32-C3 创建的 AP 热点本身不能访问互联网，部分手机会自动判断该网络不可用，从而切换到移动数据、校园网或其他已保存 WiFi。此时浏览器访问 `192.168.4.1` 的请求就不会发送到 ESP32-C3，导致网页无法打开。

### 2. 提交 WiFi 信息后页面丢失或无响应

在偶尔成功进入配网页面后，填写 Windows 移动热点名称和密码并提交，网页可能会直接丢失、卡住或无响应，无法从网页端判断 WiFi 信息是否已经提交成功。

该现象可能与 ESP32-C3 保存 WiFi 配置后重启有关。ESP32-C3 重启时会关闭原来的 AP 热点，手机与 ESP32-C3 的连接会断开，因此网页端会显示页面丢失。另外，当 ESP32-C3 从 AP 模式切换到 STA 模式后，原来的 `192.168.4.1` 地址不再可用，也会导致网页访问中断。

### 3. 校园网覆盖环境造成间接干扰

虽然手机访问 `http://192.168.4.1` 理论上只依赖 ESP32-C3 自建热点，并不需要连接校园网，但在校园网覆盖较强的环境下，仍可能间接影响 AP 配网过程。

主要原因是手机系统会优先选择可访问互联网的网络。当 ESP32-C3 热点无法上网时，手机可能自动切换到校园网、移动数据或其他已保存网络，导致配网页面访问中断。此外，校园网大量 2.4GHz 无线信号也可能造成信道拥挤，使 ESP32-C3 AP 热点连接不稳定，表现为网页加载慢、提交失败或连接断开。

## 三、解决方法

### 1. 保证手机稳定连接 ESP32-C3 配网热点

测试时应确认手机已经真正连接到 ESP32-C3 创建的 AP 热点，并在浏览器中明确输入：

```text
http://192.168.4.1
```

注意不要使用 `https://192.168.4.1`。如果手机提示该 WiFi 无法访问互联网，应选择“仍然连接”或“保持连接”。必要时关闭手机移动数据，避免手机自动切换到其他网络。

### 2. 通过串口监视器判断 ESP32-C3 状态

由于网页提交后可能因为 ESP32-C3 重启或切换网络模式而丢失，因此不能只依赖网页端判断配网是否成功。调试时应重点观察串口监视器输出，确认是否出现以下信息：

```text
WiFi 信息已提交
WiFi 配置已保存
ESP32 正在重启
正在连接已保存 WiFi
WiFi 连接成功
获得 IP 地址
```

如果串口显示 ESP32-C3 已读取保存的热点信息并成功获得 IP 地址，则说明配网成功。网页丢失在这种情况下属于正常现象。

### 3. 减少校园网和手机自动切换影响

在校园网覆盖较强的环境下，应尽量避免手机自动连接校园网。测试时可以临时关闭校园网自动连接，或暂时忘记校园网；同时关闭移动数据，使手机保持连接 ESP32-C3 配网热点。也可以将手机和 ESP32-C3 靠近一些，减少 2.4GHz 信号干扰。如果手机端仍然不稳定，可以改用电脑连接 ESP32-C3 配网热点进行测试。

## 四、配网方式优化：自动扫描附近 WiFi 并选择连接

为了解决手动输入 WiFi 名称容易出错、操作不直观的问题，后续对配网页面功能进行了优化。原来的方式是用户在网页中手动输入目标 WiFi 的 SSID 和密码，如果 WiFi 名称输入错误、大小写不一致或包含特殊字符，就会导致连接失败。

优化后的方案是在 ESP32-C3 进入 AP 配网模式后，先自动扫描附近可用 WiFi，并将扫描到的 WiFi 名称显示在配网页面中。用户进入网页后，不再需要手动输入热点名称，只需要在列表中直接选择目标 WiFi，例如 Windows 移动热点名称，然后输入对应密码并提交即可。

优化后的配网流程如下：

```text
ESP32-C3 启动 AP 配网模式
↓
自动扫描附近 WiFi 热点
↓
在配网页面显示 WiFi 列表
↓
用户直接选择目标 WiFi
↓
用户输入密码并提交
↓
ESP32-C3 保存 WiFi 信息
↓
ESP32-C3 重启并连接目标热点
↓
串口输出连接结果和 IP 地址
```

该优化具有以下作用：

第一，减少用户手动输入 SSID 的错误。用户直接从扫描列表中选择热点，避免因名称拼写错误、大小写错误或空格问题导致连接失败。

第二，提高配网操作的直观性。用户可以直接看到附近可用 WiFi，确认 Windows 热点是否已经被 ESP32-C3 扫描到，从而判断热点是否正常开启。

第三，便于排查连接问题。如果目标热点没有出现在列表中，说明 ESP32-C3 当前没有扫描到该 WiFi，可能是热点未开启、距离过远、信号弱或频段不兼容。这样可以更快定位问题，而不是盲目反复输入账号密码。

## 五、最终调试结果

经过多次测试和排查后，ESP32-C3 AP WiFi 配网网页门户最终能够成功完成配网。ESP32-C3 可以通过配网页面接收目标 WiFi 信息，保存后重启，并在重启后读取已保存的 WiFi 配置，自动连接目标热点。

串口输出中能够显示 ESP32-C3 正在连接已保存 WiFi，连接成功后输出 IP 地址、网关、信号强度等信息，说明设备已经成功接入目标网络。该结果表明，AP WiFi 配网网页门户方案基本可行，并且通过增加附近 WiFi 自动扫描和网页选择功能，可以进一步提高配网成功率和用户操作体验。

## 六、总结

本次测试中，ESP32-C3 AP WiFi 配网网页门户主要出现了页面无法稳定打开、提交后页面丢失或无响应、校园网覆盖环境干扰等问题。经过分析，这些问题主要与手机自动切换网络、ESP32-C3 保存配置后重启、AP 模式切换 STA 模式以及周围 2.4GHz 无线环境有关。

通过保持手机稳定连接 ESP32-C3 热点、关闭移动数据、避免校园网自动连接、使用串口监视器确认设备状态等方法，最终完成了 WiFi 配网连接。同时，将配网方式优化为自动扫描附近 WiFi 并在网页中选择目标热点，减少了手动输入 SSID 的错误，提高了配网操作的可靠性和可用性，为后续 ESP32-C3 连接 Windows 移动热点、进行 MQTT 联调和传感器数据上传奠定了基础。
