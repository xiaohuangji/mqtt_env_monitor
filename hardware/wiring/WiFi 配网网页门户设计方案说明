# ESP32 WiFi 配网网页门户设计方案说明

## 1. 方案概述

本系统采用 **WiFi 配网网页门户** 方案完成 ESP32-C3 的无线网络连接设计。该方案也可以称为 **AP 配网、Web 配网、网页配网门户**，属于 ESP32 项目中较常用的免硬编码 WiFi 账号密码方案。

传统 ESP32 程序中，WiFi 名称和密码通常直接写在代码中，例如：

```cpp
const char* ssid = "WiFi名称";
const char* password = "WiFi密码";
```

这种方式虽然实现简单，但是一旦更换网络环境，就必须重新修改代码并重新烧录程序，不适合实际部署和多场景使用。为了解决这个问题，本项目采用 WiFi 配网网页门户方案，使 ESP32-C3 在没有固定写死 WiFi 信息的情况下，也能够通过网页完成 WiFi 配置。

WiFi 配网网页门户的基本思想是：ESP32-C3 启动后，先检查内部是否保存过 WiFi 配置信息。如果已经保存过 WiFi 名称和密码，则自动尝试连接该 WiFi；如果没有保存过 WiFi 信息，或者保存的 WiFi 无法连接，则 ESP32-C3 自动开启一个临时 AP 热点，并启动一个网页服务器。用户使用手机或电脑连接 ESP32-C3 创建的热点后，通过浏览器访问配网页面，在网页中输入目标 WiFi 的名称和密码。ESP32-C3 接收到这些信息后，将其保存到内部存储中，并切换到 STA 模式连接目标 WiFi。

这种方式可以避免在程序中直接写死 WiFi 账号和密码，使同一份程序能够适配不同网络环境，提高系统的灵活性和可维护性。

---

## 2. AP 配网与 STA 工作模式

ESP32 的 WiFi 常用工作模式包括 **STA 模式** 和 **AP 模式**。

**STA 模式** 是指 ESP32 作为普通无线终端连接到已有 WiFi 网络中。例如 ESP32-C3 连接手机热点、实验室路由器或宿舍 WiFi 时，就是 STA 模式。在 STA 模式下，ESP32-C3 会从路由器或手机热点获得一个局域网 IP 地址。后续进行 MQTT 数据上传、NTP 时间同步、HTTP 通信或局域网访问时，都需要依赖该模式。

**AP 模式** 是指 ESP32 自己创建一个 WiFi 热点，让手机或电脑连接它。这个热点通常只用于临时配置网络，不用于长期传输传感器数据。用户连接 ESP32-C3 创建的临时热点后，可以通过浏览器访问 ESP32-C3 内部运行的 WebServer 页面，在网页中输入目标 WiFi 的 SSID 和密码。

在本项目中，ESP32-C3 正常工作时采用 STA 模式连接目标 WiFi；当设备没有保存 WiFi 信息，或者连接保存的 WiFi 失败时，则进入 AP 配网模式，开启临时热点和网页门户。

---

## 3. WiFi 配网网页门户工作流程

本系统的 WiFi 配网流程如下：

```text
ESP32-C3 上电启动
↓
初始化 WiFi 配网模块
↓
读取内部是否保存过 WiFi 配置信息
↓
如果存在已保存 WiFi，则尝试自动连接
↓
连接成功后进入正常工作模式
↓
如果没有保存信息或连接失败，则开启 AP 配网热点
↓
用户使用手机或电脑连接 ESP32-C3 临时热点
↓
用户在浏览器中访问配网页面
↓
用户输入目标 WiFi 名称和密码
↓
ESP32-C3 保存 WiFi 配置信息
↓
ESP32-C3 切换到 STA 模式并连接目标 WiFi
↓
连接成功后获取 IP 地址
↓
进行 NTP 北京时间同步
↓
开始传感器数据采集、JSON 封装和串口输出
```

在实际运行中，ESP32-C3 启动后会先尝试连接已经保存的 WiFi。如果连接成功，串口会输出 WiFi 连接状态、SSID、IP 地址、网关、子网掩码、DNS、MAC 地址和 RSSI 信号强度等信息。WiFi 连接成功后，系统继续进行 NTP 时间同步，使后续传感器数据能够带有真实北京时间戳。随后系统初始化传感器，并按照设定周期采集温度、湿度、土壤湿度和 MQ-2 气体传感器数据，将其封装成 JSON 格式，并生成对应的 Topic。

如果 ESP32-C3 检测到 WiFi 断开，程序会暂停传感器采集，进入自动重连流程。重连成功后，系统继续采集数据并输出。这样的设计可以避免网络异常时继续产生无法上传的数据，也为后续接入 MQTT Broker 和数据库存储提供了基础。

---

## 4. 网页门户的作用

WiFi 配网网页门户本质上是 ESP32-C3 内部运行的一个小型 Web 服务器。ESP32-C3 在 AP 配网模式下会提供一个简单网页，用户打开网页后可以看到 WiFi 配置表单，例如：

```text
WiFi SSID:
[                ]

WiFi Password:
[                ]

[保存并连接]
```

用户在网页中输入目标 WiFi 名称和密码后，ESP32-C3 会接收网页提交的数据，并将 WiFi 信息保存到内部非易失性存储空间中。保存完成后，设备会尝试连接目标 WiFi。如果连接成功，系统进入正常工作状态；如果连接失败，可以继续进入配网页面重新输入信息。

网页门户的主要作用包括：

1. 提供 WiFi 配置入口，使用户不需要修改代码即可配置网络。
2. 降低设备使用难度，使非开发人员也能通过手机或电脑完成配网。
3. 提高设备在不同网络环境中的适应能力，便于后续部署和调试。

---

## 5. 本项目中的 WiFi 设计说明

结合本项目 ESP32-C3 环境监测节点的功能需求，WiFi 配网模块主要承担以下任务：

首先，系统启动后初始化 WiFi 配网模块，并读取是否存在已保存的 WiFi 配置信息。如果检测到已经保存了 WiFi 名称，例如手机热点或路由器名称，则 ESP32-C3 自动尝试连接该 WiFi。连接过程中，串口输出连接提示信息，连接成功后输出当前网络的基本信息。

其次，WiFi 连接成功后，系统进行 NTP 北京时间同步。同步成功后，后续每条传感器数据都可以使用真实时间戳，例如：

```text
2026-06-11T16:25:57+08:00
```

这比使用 `local_time_3s` 这类本地运行时间更加准确，便于后续进行 24 小时数据记录、数据库存储和环境变化分析。

再次，传感器采集阶段会结合 WiFi 状态进行判断。如果 WiFi 正常连接，则系统继续采集 DHT11 温湿度传感器、土壤湿度检测模块和 MQ-2 气体传感器的数据，并生成对应 Topic 和 JSON Payload。如果 WiFi 断开，系统会暂停采集，并尝试自动重连。重连成功后继续采集，保证系统具有一定的容错能力。

当前 MQTT 功能虽然暂未启用，但 Topic 和 Payload 已经按照后续 MQTT 上传格式准备好。后续只需要在 WiFi 正常连接的基础上接入 MQTT Broker，即可实现无线数据上传。

---

## 6. 方案优势

### 6.1 不需要硬编码 WiFi 账号和密码

采用 WiFi 配网网页门户后，ESP32-C3 程序中不需要固定写入某一个 WiFi 名称和密码。设备更换网络环境时，不需要重新修改代码，也不需要重新烧录程序，只需要通过网页重新输入 WiFi 信息即可。

这种方式提高了程序的通用性。对于环境监测系统来说，设备可能会在宿舍、实验室、教室或手机热点等不同网络环境中使用。如果采用硬编码方式，每换一个网络就要重新修改程序，会增加调试和维护成本。而网页配网方式可以让同一份程序适用于多个场景。

### 6.2 使用灵活，便于实际部署

AP 配网方式操作相对直观。ESP32-C3 在需要配网时自动开启一个临时热点，用户只需要使用手机或电脑连接该热点，然后访问配网页面输入目标 WiFi 信息即可完成配置。

这种方式接近许多智能家居设备的配网流程，适合实际工程部署。对于课程设计项目来说，该方案也能体现系统的工程化设计能力，使设备不仅能在开发者电脑旁运行，也能在不同环境中快速重新配置使用。

### 6.3 便于后续功能扩展

WiFi 是 ESP32-C3 环境监测系统实现无线通信的基础。连接成功后，系统可以继续实现 NTP 时间同步、MQTT 数据上传、HTTP 数据提交、Web 监控、数据库存储等功能。

在本项目中，WiFi 连接成功后，ESP32-C3 可以获取真实北京时间，并将传感器数据封装为带时间戳的 JSON 格式。后续接入 MQTT Broker 后，可以直接将当前串口中已经验证过的 Topic 和 Payload 发布出去。因此，WiFi 配网网页门户为后续无线数据传输和远程监控奠定了基础。

---

## 7. 可能存在的问题

### 7.1 首次配网需要用户手动操作

WiFi 配网网页门户虽然避免了硬编码 WiFi 信息，但第一次使用时需要用户手动连接 ESP32-C3 创建的临时 AP 热点，并通过浏览器进入配网页面输入 WiFi 名称和密码。对于不熟悉 ESP32 配网流程的用户来说，操作步骤可能不够直观。

如果没有清晰的串口提示或使用说明，用户可能不知道应该连接哪个热点，也不知道配网页面的访问地址。因此，首次配网的操作复杂度比直接在代码中写死 WiFi 信息稍高。

### 7.2 手机可能自动断开 ESP32 配网热点

ESP32-C3 在 AP 配网模式下创建的临时热点通常不能访问互联网。部分手机在连接这类热点时，可能会提示“当前网络无法访问互联网”，并自动切换到移动数据或其他可上网的 WiFi 网络。

如果手机自动断开 ESP32-C3 的配网热点，用户就无法正常访问配网页面，或者在提交 WiFi 信息时出现失败。这是 AP 配网页面在手机端调试时比较常见的问题。

### 7.3 安全性和异常处理需要额外设计

如果 ESP32-C3 的配网热点长期开放，附近其他设备也可能连接该热点并访问配网页面，存在一定安全风险。同时，如果已经保存的 WiFi 名称或密码失效，例如路由器密码被修改、手机热点关闭、SSID 改变等，ESP32-C3 可能会连接失败。

如果程序没有设计连接超时、自动重连或重新进入配网模式，设备可能会长时间停留在连接失败状态，影响后续传感器采集和数据上传。因此，该方案需要在程序中加入异常处理机制。

---

## 8. 问题解决方案

### 8.1 针对首次配网手动操作的解决方案

为了解决首次配网操作不直观的问题，系统可以在未检测到已保存 WiFi 信息时，自动开启固定名称的 AP 配网热点，并通过串口输出明确提示信息。例如：

```text
[WiFi] No saved WiFi config.
[WiFi] Start AP config portal.
[WiFi] Please connect to WiFi: ESP32_ENV_CONFIG
[WiFi] Open browser and visit: 192.168.4.1
```

这样用户只需要按照串口提示进行操作即可。ESP32-C3 创建的热点名称可以设置为固定名称，例如：

```text
ESP32_ENV_CONFIG
```

配网页面地址可以使用 AP 模式下的固定地址：

```text
192.168.4.1
```

同时，可以在项目文档中写明配网步骤：先连接 ESP32-C3 热点，再打开浏览器访问 192.168.4.1，最后输入目标 WiFi 名称和密码并提交。通过清晰的提示和说明，可以降低首次配网难度。

### 8.2 针对手机自动断开热点的解决方案

针对部分手机可能自动断开 ESP32-C3 配网热点的问题，可以在调试说明中提醒用户：连接 ESP32-C3 配网热点时，如果手机提示该网络无法访问互联网，应选择“仍然连接”或“保持连接”。必要时可以临时关闭手机移动数据，避免手机自动切换网络。

同时，ESP32-C3 应提供固定配网页面地址，例如：

```text
192.168.4.1
```

用户连接 ESP32-C3 热点后，可以直接在浏览器中输入该地址进入配网页面。

如果后续需要进一步提高配网体验，可以增加 DNS 重定向功能，也就是常见的“强制门户”效果。这样用户连接 ESP32-C3 热点后，即使打开任意网页，也可以自动跳转到配网页面，从而减少手动输入地址的步骤，提高配网成功率。

### 8.3 针对安全和异常处理的解决方案

针对安全问题，可以为 ESP32-C3 的配网热点设置密码，避免附近无关设备随意连接。例如：

```text
热点名称：ESP32_ENV_CONFIG
热点密码：12345678
```

同时，ESP32-C3 不应长期开放 AP 热点。设备只有在没有 WiFi 配置、连接失败或用户主动重新配网时才开启 AP 配网模式。连接目标 WiFi 成功后，应关闭 AP 热点，只保留 STA 模式运行，从而降低安全风险和功耗。

针对连接异常问题，程序应设置 WiFi 连接超时机制。例如启动后尝试连接已保存 WiFi，如果 15 秒内仍未连接成功，则自动进入 AP 配网模式，而不是一直停留在连接等待状态。流程可以设计为：

```text
读取已保存 WiFi 信息
↓
尝试连接 15 秒
↓
连接成功：进入正常采集模式
↓
连接失败：开启 AP 配网页面
```

此外，系统运行过程中还应加入断线自动重连机制。当 WiFi 连接丢失时，ESP32-C3 暂停传感器采集并尝试自动重连；重连成功后恢复采集。如果长时间无法重连，则重新进入配网模式。这样可以提高设备在长时间运行过程中的稳定性。

---

## 9. 总结

本系统采用 WiFi 配网网页门户方案完成 ESP32-C3 的无线网络连接设计。该方案通过 AP 配网和 Web 配网页面的方式，使 ESP32-C3 不需要在程序中硬编码 WiFi 名称和密码。设备启动后会优先尝试连接已经保存的 WiFi，如果连接失败或没有配置信息，则自动开启临时 AP 热点和网页门户，用户可以通过手机或电脑完成 WiFi 配置。

该方案的主要优势在于：不需要硬编码 WiFi 信息，便于设备更换网络环境；操作灵活，适合实际部署；并且能够为后续 NTP 时间同步、MQTT 数据上传、数据库存储和远程监控功能提供网络基础。

同时，该方案也存在一些问题，例如首次配网需要手动操作、部分手机可能自动断开无法上网的 ESP32 配网热点，以及配网热点安全性和连接异常处理问题。针对这些问题，系统可以通过增加串口提示和操作文档、提醒用户保持热点连接、设置固定配网页面地址、增加 DNS 重定向、为配网热点设置密码、连接成功后关闭 AP 热点、加入连接超时和断线重连机制等方式进行优化。

综上，WiFi 配网网页门户方案适合本项目的 ESP32-C3 环境监测节点。它能够提高设备的网络适应能力和工程实用性，使系统在不同 WiFi 环境下都可以方便地重新配置和使用，并为后续实现 24 小时数据采集、MQTT 上传和数据库保存奠定基础。
