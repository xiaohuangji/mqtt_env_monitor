# ESP32-C3 传感器串口输出与 WiFi 连接过程总结

本次调试主要完成了 ESP32-C3 环境监测节点的传感器串口输出验证和 WiFi 连接过程验证。通过串口监视器可以看到，程序已经能够正常启动，完成 WiFi 配网模块初始化、已保存 WiFi 信息读取、自动连接热点、传感器初始化以及环境数据采集输出等过程。整体运行结果表明，ESP32-C3 硬件节点的基础功能已经基本实现，为后续 MQTT 上传、数据库存储和 24 小时数据记录打下了基础。

在传感器串口输出方面，系统采用 ESP32-C3 作为核心控制板，连接多个环境传感器模块。其中 GPIO0 用于连接 DHT11 温湿度传感器，GPIO1 预留给光照传感器，GPIO2 预留给噪声检测模块，GPIO3 用于连接土壤湿度检测模块，GPIO4 用于连接 MQ-2 气体传感器模块。这样的引脚分配能够满足当前温度、湿度、土壤湿度和气体检测数据采集需求，同时也为后续扩展光照和噪声检测功能保留了接口。

程序启动后，串口显示传感器采集程序已经正常运行，并提示当前模式为采集传感器数据并打印 JSON 数据。此时 WiFi 和 MQTT 可以根据调试阶段选择是否启用。在未启用 MQTT 的情况下，ESP32-C3 不会真正向服务器发送数据，而是将准备上传的数据以 Topic 和 Payload 的形式打印到串口监视器中。这样做的好处是可以先验证传感器采集结果、数据格式和主题结构是否正确，避免后续接入 MQTT 或数据库时出现格式不统一的问题。

传感器数据输出采用类似 MQTT 的结构，每一条数据都由 Topic 和 Payload 两部分组成。Topic 用于标识数据来源和数据类型，例如 `env_monitor/node03/temperature` 表示 node03 节点上传的温度数据，`env_monitor/node03/humidity` 表示湿度数据，`env_monitor/node03/soil_moisture` 表示土壤湿度数据，`env_monitor/node03/mq2_gas` 表示 MQ-2 气体检测数据。通过这种主题命名方式，可以清楚区分不同节点和不同类型的数据，便于后续 MQTT 订阅、分类处理和数据库存储。

Payload 部分采用 JSON 格式进行封装。每条数据中包含 `node_id`、`data_type`、`value`、`unit`、`timestamp` 和 `seq` 等字段。其中，`node_id` 表示节点编号，`data_type` 表示数据类型，`value` 表示传感器采集值，`unit` 表示单位，`timestamp` 表示时间戳，`seq` 表示数据序号。这样的数据结构清晰、统一，便于串口调试，也便于后续通过 MQTT 或其他通信方式上传到服务器。

从传感器数据来看，DHT11 能够正常采集温度和空气湿度，温度单位为摄氏度，湿度单位为百分比。土壤湿度模块能够输出土壤湿度百分比，用于反映当前土壤干湿程度。MQ-2 气体传感器输出的是 ADC 原始值，单位为 raw，表示当前气体传感器检测到的原始模拟采样结果。该值可以用于判断气体或烟雾浓度变化趋势，但在没有标准标定的情况下，不能直接表示为 ppm 浓度。

在时间戳方面，早期未连接 WiFi 时，程序使用类似 `local_time_3s`、`local_time_6s` 的运行时间作为临时时间戳，表示数据是在程序运行后的第几秒采集的。后续启用 WiFi 和 NTP 时间同步后，系统可以获取真实北京时间，并将真实时间写入每条 JSON 数据中，例如 `2026-06-11T16:25:57+08:00`。这样可以更准确地记录每条数据的采集时间，为后续 24 小时数据统计和历史数据分析提供依据。

在 WiFi 连接过程方面，系统采用 WiFi 配网网页门户方案。ESP32-C3 启动后会先初始化 WiFi 配网模块，并检查内部是否保存过 WiFi 信息。如果已经保存过热点名称和密码，程序会自动尝试连接该 WiFi。例如串口中显示尝试连接已保存的热点 `OPPO Reno15 E11C`，连接过程中会连续输出点号，表示正在等待连接结果。当连接成功后，串口会显示连接成功提示，并输出 ESP32-C3 获得的 IP 地址。

WiFi 连接成功后，系统会输出详细的网络状态信息，包括 WiFi 工作模式、连接状态、SSID、IP 地址、网关、子网掩码、DNS、MAC 地址和 RSSI 信号强度等。其中，IP 地址是 ESP32-C3 在当前局域网中的地址，后续如果需要通过局域网访问设备或进行通信调试，可以使用该地址。RSSI 表示 WiFi 信号强度，数值越接近 0 表示信号越强。串口中显示的 RSSI 数值较好，说明 ESP32-C3 与热点之间连接质量较稳定。

在 WiFi 配网页面运行过程中，串口中可能会出现 `request handler not found` 的提示。这通常不是严重错误，而是手机或浏览器自动访问了一些 ESP32 WebServer 没有处理的路径，例如网页图标、网络检测页面等。只要配网页面能够正常打开，WiFi 信息能够正常提交，并且设备最终能够连接成功，这类提示一般不会影响系统运行。

串口中还出现过 ESP32-C3 的重启启动日志，例如 `ESP-ROM`、`rst`、`boot` 等内容。这些是开发板重启时自动打印的芯片启动信息，表示 ESP32-C3 正在从 Flash 中重新加载程序。如果是在保存 WiFi 配置信息后重启，或者手动按下复位键后出现这些信息，属于正常现象，不代表程序出错。

WiFi 连接成功后，ESP32-C3 可以进一步进行 NTP 时间同步。串口中显示 NTP 同步成功后，系统能够获取当前北京时间。这个功能对于环境监测系统非常重要，因为传感器数据不仅需要数值，还需要准确的采集时间。通过真实时间戳，可以在后续数据库中按照时间顺序保存数据，也可以生成 24 小时内的环境变化曲线。

此外，程序还实现了 WiFi 断线检测和自动重连功能。当 ESP32-C3 检测到 WiFi 连接丢失时，串口会输出连接丢失提示，并记录断线发生时间。断线期间，程序会暂停传感器采集，避免在网络异常时继续产生无法上传的数据。随后系统会自动尝试重新连接 WiFi。如果重连成功，串口会显示重连成功、重连耗时、当前 SSID 和当前 IP 地址，并继续恢复传感器数据采集。

从串口输出可以看出，WiFi 断开后系统并没有直接重启，而是在运行过程中完成了断线检测、暂停采集、自动重连和恢复采集。重连成功后，传感器数据序号 seq 会继续递增，而不是从 1 重新开始，说明程序运行状态保持连续。这体现了系统具备一定的容错能力，适合后续长时间运行和无线数据传输。

总体来看，本次调试验证了 ESP32-C3 环境监测节点的两个核心部分：一是传感器数据能够被正常采集，并按照统一的 Topic 和 JSON Payload 格式通过串口输出；二是 WiFi 模块能够自动连接已保存热点，并在连接成功后输出网络信息、同步北京时间，同时具备断线检测和自动重连能力。当前系统虽然还没有真正启用 MQTT 上传，但 Topic 和 Payload 已经按照后续 MQTT 发布格式设计完成，因此后续只需要在现有基础上加入 MQTT 客户端连接和发布逻辑，就可以将串口中显示的数据真正上传到 MQTT Broker 或服务器端。

本阶段调试结果说明，ESP32-C3 硬件节点已经完成了从“传感器采集”到“数据封装”再到“联网准备”的基础流程。后续可以继续完善 MQTT 数据发布、数据库存储、Web 页面展示和 24 小时数据统计分析功能，使系统逐步形成完整的无线环境监测平台。
