# -*- coding: utf-8 -*-
"""生成胡艺瀚《数据通信与网络课程设计》课程设计日志 docx（对齐孙博宇日志格式）。
封面信息块 + 10 个工作日（每日 6 小节）+ 个人总结（5 小节）+ 3 张表。
运行：python scripts/build_log_huyihan.py
"""
import os
from docx import Document
from docx.shared import Pt, Cm, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.enum.table import WD_TABLE_ALIGNMENT
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

CIRCLED = "①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬⑭⑮"

SRC = r"C:\Users\28445\xwechat_files\wxid_9relx9qlac3112_8721\msg\file\2026-06\《数据通信与网络技术》课设日志.docx"
OUT = "docs/team/daily/胡艺瀚_课程设计日志.docx"
doc = Document(SRC)  # 只读孙博宇原件（含其封面：校徽/校名/排版），改完另存为 OUT；下面删正文、改字段


def set_font(run, cn="宋体", en="Times New Roman", size=12, bold=False, color=(0, 0, 0)):
    run.font.size = Pt(size)
    run.font.bold = bold
    run.font.color.rgb = RGBColor(*color)
    run.font.name = en
    rpr = run._element.get_or_add_rPr()
    rf = rpr.find(qn("w:rFonts"))
    if rf is None:
        rf = OxmlElement("w:rFonts"); rpr.append(rf)
    rf.set(qn("w:eastAsia"), cn); rf.set(qn("w:ascii"), en); rf.set(qn("w:hAnsi"), en)


def info(text):  # 日期行：Normal 样式，字号继承孙博宇
    doc.add_paragraph().add_run(text)


def h3(text):  # 工作日标题：用孙博宇的 Heading 3 样式（16pt），不覆盖字体字号
    doc.add_paragraph(text, style="Heading 3")


def label(text):  # 小节标题：Normal + 加粗，字号继承
    r = doc.add_paragraph().add_run(text); r.bold = True


def body(text):  # 正文：无首行缩进、1.35 行距，字号继承（与孙博宇一致）
    p = doc.add_paragraph(); p.paragraph_format.line_spacing = 1.35
    p.add_run(text)


def items(lst):  # ①②③ 条目：Normal，字号继承
    for i, t in enumerate(lst):
        p = doc.add_paragraph(); p.paragraph_format.line_spacing = 1.35
        pre = CIRCLED[i] if i < len(CIRCLED) else "·"
        p.add_run(pre + " " + t)


def sep():  # 工作日之间的分割横线（复刻孙博宇：空段 + 下边框 single/sz6/space1 + 段后 6pt）
    p = doc.add_paragraph()
    pPr = p._p.get_or_add_pPr()
    pBdr = OxmlElement("w:pBdr")
    bottom = OxmlElement("w:bottom")
    bottom.set(qn("w:val"), "single"); bottom.set(qn("w:sz"), "6")
    bottom.set(qn("w:space"), "1"); bottom.set(qn("w:color"), "auto")
    pBdr.append(bottom); pPr.append(pBdr)
    sp = OxmlElement("w:spacing"); sp.set(qn("w:after"), "120"); pPr.append(sp)


def _grid(t):
    tblPr = t._tbl.tblPr
    b = OxmlElement("w:tblBorders")
    for edge in ("top", "left", "bottom", "right", "insideH", "insideV"):
        e = OxmlElement("w:" + edge)
        e.set(qn("w:val"), "single"); e.set(qn("w:sz"), "4"); e.set(qn("w:space"), "0"); e.set(qn("w:color"), "000000")
        b.append(e)
    tblPr.append(b)


def table(headers, rows, widths=None):
    t = doc.add_table(rows=1, cols=len(headers)); t.alignment = WD_TABLE_ALIGNMENT.CENTER
    try:
        t.style = "Table Grid"
    except KeyError:
        pass
    _grid(t)
    for i, h in enumerate(headers):
        c = t.rows[0].cells[i]; c.paragraphs[0].alignment = WD_ALIGN_PARAGRAPH.CENTER
        set_font(c.paragraphs[0].add_run(h), "黑体", "Arial", 9, bold=True)
    for row in rows:
        cells = t.add_row().cells
        for i, v in enumerate(row):
            set_font(cells[i].paragraphs[0].add_run(str(v)), "宋体", "Times New Roman", 9)
    if widths:
        for i, w in enumerate(widths):
            for r in t.rows:
                r.cells[i].width = Cm(w)
    doc.add_paragraph().paragraph_format.space_after = Pt(2)


# ============ 复用孙博宇封面：删掉他的正文、改封面字段 ============
# 1) 删除孙博宇正文（从他的“第 1 个工作日”到结尾，保留封面与节属性 sectPr）
_body = doc.element.body
_elems = list(_body)
_start = None
for _i, _el in enumerate(_elems):
    if _el.tag == qn("w:p"):
        _txt = "".join((_t.text or "") for _t in _el.iter(qn("w:t")))
        if _txt.strip().startswith("第 1 个工作日"):
            _start = _i; break
if _start is not None:
    for _el in _elems[_start:]:
        if _el.tag in (qn("w:p"), qn("w:tbl")):
            _body.remove(_el)
# 2) 改封面字段（仅替换冒号后的值，保留原标签与格式）
_COVER = {"学生姓名": "胡艺瀚", "学号": "2023905925", "班级": "2023240303",
          "小组成员": "孙博宇、韦煜城、曾家豪", "本人主要分工": "软件设计、性能测试与项目推进"}
def _set_text(p, text):
    if p.runs:
        p.runs[0].text = text
        for r in p.runs[1:]:
            r.text = ""
    else:
        p.add_run(text)
for p in doc.paragraphs:
    if "：" not in p.text:
        continue
    _lab = p.text.split("：", 1)[0]
    _key = _lab.replace(" ", "").replace("　", "")
    if _key in _COVER:
        _set_text(p, _lab + "：" + _COVER[_key])

# ===================== 工作日 =====================
DAYS = [
 ("第 1 个工作日：题目理解、项目规划与需求设计", "2026年6月8日",
  ["今天主要完成课程设计项目的启动与整体规划。上午认真听取老师对课程设计的讲解，了解整体安排、工作日进度、阶段检查、报告结构、答辩方式和测试证据要求；老师强调要体现工程思维，从需求分析、方案设计、协议选择、测试验证到过程记录完整说明设计依据。课后对本组选题进行分析，组织并参与第一次小组会议，讨论应用场景、技术路线、分工与性能目标。"],
  ["梳理课程设计的阶段要求、提交材料和答辩重点。",
   "主导构建项目整体推进流程，确定“一个模块一个模块推进”的方式。",
   "规划 GitHub 协作流程，通过 Issue、commit、README 进度和文档进行过程留痕。",
   "组织第一次小组会议，明确分工：本人和曾家豪负责软件侧，孙博宇和韦煜城负责硬件侧。",
   "完成项目基础目录结构搭建、课程资料归档与 README 初版。",
   "完成《需求分析与设计指标》初稿，以及《MQTT 主题与消息格式设计》文档（Topic 命名、JSON 字段与中文显示映射）。"],
  ["项目初期任务较多，一次性规划过大容易导致后续执行压力过大。",
   "设计 MQTT Topic 和 JSON 字段时，需要判断是否可使用中文命名。"],
  ["采用“一个模块一个模块推进”的流程，每次只围绕当前模块完成设计、实现和提交。",
   "确定文档和页面展示用中文，但 Topic、JSON 字段和代码变量统一用英文，减少编码兼容与抓包分析问题。"],
  ["完成项目仓库初始结构、README 初版，以及需求分析与 MQTT 协议设计文档。",
   "主导确定项目整体流程与小组分工，并整理第一次会议记录。"],
  ["推进 GitHub 协作流程规范。",
   "进行本地 MQTT Broker 方案调研，为 PC 模拟节点和监控端开发做准备。"]),

 ("第 2 个工作日：协作流程规范与本地 Broker 验证", "2026年6月9日",
  ["今天主要围绕 GitHub 协作流程规范和软件侧开发准备开展工作。对小组成员提交的 Issue 和 PR 进行审查，进一步规范“先 Issue、再 PR、审查后合并”的流程；并编写本地 MQTT Broker 与发布订阅测试脚本，为 PC 模拟节点和 Web 监控端开发打基础。"],
  ["审查队友提交的 ESP32 硬件节点设计、工程化分析等 Issue 和 PR，整理可复制到评论区的修改建议（文件名补 .md、PR 关联 Issue、文档范围压缩、协议一致性检查等）。",
   "处理 GitHub 分支同步与推送问题，熟悉 fetch、pull --ff-only、merge、push 等操作。",
   "新增 requirements.txt，记录 paho-mqtt 与 amqtt 依赖。",
   "编写 scripts/mqtt_broker.py 启动本地轻量 Broker，以及发布/订阅测试脚本。",
   "用 Python 脚本完成 2 个节点、4 类环境数据共 8 条消息的发布订阅验证。"],
  ["本地 main 与远程 main 存在差异，直接 push 被拒绝。",
   "队友的 Issue 和 PR 存在任务范围偏大、文件名缺后缀、PR 与 Issue 未关联等问题。"],
  ["先 fetch 再用 pull --ff-only 将本地快进到远程最新，理解了远程更新时不能直接推送的原因。",
   "采用先审查、再评论建议、最后由对应负责人修改的方式处理队友 PR，既规范又保留各自工作过程。"],
  ["完成 Issue 与 PR 审查，明确队友 PR 合并前需修改的问题。",
   "完成本地 Broker 与发布订阅测试脚本，成功收发 8 条测试消息。"],
  ["实现 PC 模拟环境监测节点。",
   "实现 Web 监控端订阅显示功能。"]),

 ("第 3 个工作日：PC 模拟节点与 Web 监控端实现", "2026年6月10日",
  ["今天是中期检查前的最后一个工作日，主要完成软件侧两个核心模块——PC 模拟节点和 Web 监控端，并审查合并队友的 ESP32 设计文档。目标是让“模拟节点 → Broker → 监控端显示”的完整软件链路真正跑起来，为中期检查提供可演示、可验证的成果。"],
  ["实现 PC 模拟节点 sim_node.py，支持节点编号、数据类型、上传周期和 QoS 等命令行配置。",
   "采用均值回归随机游走生成温湿度、光照、噪声等数据，使其平滑变化、贴近真实环境。",
   "实现断线自动重连与 QoS 1 离线排队补发，经双节点并发与 Broker 重启验证重连能力。",
   "实现 Web 监控端 web_monitor.py，基于 Flask + SSE 网页实时刷新，无需手动刷新。",
   "实现节点编号、数据类型中文映射、数值、单位、上传/接收时间显示，记录毫秒级接收日志，15 秒无数据自动标记离线。",
   "编写两模块的运行验证文档；审查并合并队友孙博宇的两个 ESP32 设计 PR，合并前指出文件名与数据类型口径问题。"],
  ["纯随机数生成的模拟数据会无规律剧烈跳变，不像真实环境数据。",
   "监控端需要在不刷新页面的情况下实时更新数据。"],
  ["采用均值回归随机游走，每步向基准值回归并叠加高斯噪声，使数据在合理范围内平滑漂移。",
   "采用 SSE，由后端在收到新消息时主动推送到浏览器。"],
  ["完成 PC 模拟节点（Issue #16）与 Web 监控端（Issue #18）及验证文档。",
   "打通“模拟节点 → Broker → 监控端网页显示”完整软件链路，并完成队友两个 ESP32 PR 的审查合并。"],
  ["参加课程设计中期检查。",
   "根据老师检查意见调整后续方案。"]),

 ("第 4 个工作日：中期检查、负载建模、MQTT 升级与性能实验", "2026年6月11日",
  ["今天上午参加中期检查。老师指出当前方案功能完整，但还需体现“方案要由需求驱动”和“要充分考虑成本”。下午围绕反馈重新梳理工作，确定“需求建模 → 实验验证 → 需求计算器”的新主线，并开始密集推进网络负载建模、Broker 升级与性能实验。"],
  ["完成中期检查汇报。",
   "建立“需求参数 → 网络负载 → 服务器配置 → 成本”的通用计算方法，完成《网络负载与服务器配置计算方法建模》（Issue #24）。",
   "完成 MQTT 方案升级（Issue #25）：Broker 由实验性 amqtt 升级为生产级 EMQX 5.8.9，客户端升级 MQTT 5.0（Topic Alias、会话过期），新增 json / json-min / msgpack 三种 payload 格式。",
   "因本地 Windows 不便做大规模压测，租用一台阿里云服务器作为实验台架，部署 EMQX 与 emqtt_bench。",
   "完成性能实验（Issue #27）：协议开销抓包矩阵、EMQX 容量与吞吐压测（5 万连接 / 5 万条每秒）、amqtt 对照、跨公网延迟实测，并把实测系数回填建模文档。",
   "完成需求计算器（Issue #33）：监控端新增 /calculator 页面，输入需求即输出负载、配置与成本。",
   "补充场景实景压测与稳定性验证：2 万连接 30 分钟零掉线、转发送达 99.96%、断线重连 5.2 秒。"],
  ["老师指出原方案缺少明确的需求依据与成本分析。",
   "本地笔记本不适合做几万连接压测，作为容量基准也不具备“可购买配置”的代表性。"],
  ["建立通用计算模型，把方案选择变成可由需求参数推导的过程，而非拍脑袋决定。",
   "用 amqtt 作对照组同条件压测，用数据对比说明 EMQX 的容量与吞吐优势；租用配置明确的云服务器，使容量曲线可对应“什么配置支撑多少节点、成本多少”。"],
  ["完成中期汇报、负载与成本建模文档、MQTT 升级、性能实验（抓包/压测/对照/延迟并校准系数）与需求计算器初版。"],
  ["补充满负载与 QoS 丢包实验，使内存建模更严谨。",
   "进一步完善需求计算器的展示与可视化。"]),

 ("第 5 个工作日：补充性能实验与计算器升级", "2026年6月12日",
  ["今天主要做两方面：一是补充完善性能实验使建模更严谨；二是为 ESP32 硬件节点接入做网络侧准备与对接规范。"],
  ["补充满负载连接爬坡实验，发现满负载每连接内存约为空闲的 2.46 倍，说明单一安全系数估算内存不够。",
   "补充 QoS × 丢包实验，发现 MQTT 跑在 TCP 之上、由 TCP 重传兜底，三种 QoS 在持续连接下到达率都接近 100%，代价是延迟随丢包率上升。",
   "将连接爬坡加密到 10 个档位，使内存拟合曲线更扎实。",
   "升级需求计算器：内存估算改为“空闲下界 ~ 满负载上界”双模型，前端改为深色仪表盘并用图表展示模型与实测对照。",
   "完成 ESP32 接入网络侧准备（Issue #34）：监控端与模拟节点新增可燃气体、土壤湿度类型映射，建模文档新增接入层容量约束，计算器新增所需 AP 数输出，并整理 ESP32 到网络端对接规范。",
   "针对队友反馈的 ESP32 配网网页不稳定（Issue #35），分析原因并建议改用成熟的 WiFiManager 库。"],
  ["早期内存模型用单一安全系数估算，与满负载实测差距较大。",
   "队友 ESP32 固件采集的传感器类型与监控端原有类型不一致。"],
  ["专门做满负载爬坡实验得到满负载内存曲线，把计算器升级为空闲~满负载双模型，选型按满负载上界取值。",
   "在监控端与模拟节点补充对应类型映射与单位，使队友数据正确显示。"],
  ["完成满负载爬坡与 QoS 丢包两项补充实验、计算器内存双模型与可视化升级、ESP32 接入网络侧准备与对接规范。"],
  ["搭建 ESP32 联调所需的服务器环境，验证从硬件节点到监控端的完整链路。"]),

 ("第 6 个工作日：EMQX 部署与端到端联调", "2026年6月13日",
  ["今天主要搭建并验证 ESP32 接入所需的服务器侧环境。考虑到方案主线是 EMQX，为使演示与报告一致，在本地用 Docker 部署 EMQX 5.8.9（与压测同版本），由笔记本扮演“未来自建部署的服务器”，并用模拟节点完整验证从设备到监控端的链路。"],
  ["在本地用 Docker 部署 EMQX 5.8.9，映射 MQTT 端口 1883 与 Dashboard 端口 18083。",
   "配置 Windows 移动热点组网，使 ESP32 与笔记本同处一个局域网，笔记本固定 IP 192.168.137.1 作为 Broker 地址。",
   "用 PC 模拟节点模拟 ESP32（完整 JSON、MQTT 3.1.1）经热点 IP 连接 EMQX，端到端验证服务器侧链路全通，监控端正确显示四类数据。",
   "编写《EMQX 部署与联调说明》，记录部署、启停、联调、验证流程与常见问题排查。"],
  ["演示用本地 Mosquitto 还是 EMQX 的一致性问题；Docker 拉取镜像报错；担心 Docker 端口映射只绑本机导致 ESP32 连不上。"],
  ["演示统一改用与压测同版本的 EMQX 5.8.9（Docker），自带 Dashboard 便于答辩；登录 Docker Hub 后重新拉取成功；用模拟节点经热点 IP 验证，确认端口映射对外可见、ESP32 可连入。"],
  ["完成本地 EMQX 5.8.9 的 Docker 部署、从设备到监控端的端到端联调验证，以及《EMQX 部署与联调说明》文档。"],
  ["等硬件队友完成 ESP32 配网与 MQTT 上传后进行真机联调，并继续推进报告与答辩材料整理。"]),

 ("第 7 个工作日：ESP32 室外长测的服务器侧记录与分析", "2026年6月15日",
  ["今天配合硬件队友的 ESP32 室外全天长测，负责服务器侧的数据记录与分析。在云端用持久会话记录器 7×24 接收并落盘，本地无需开机；事后对约 15 小时数据做到达率统计与曲线分析，并定位光照数据异常的根因（属硬件侧）。"],
  ["在云服务器部署 cloud_recorder.py（QoS 1、持久会话、systemd 常驻），持续记录 ESP32 上报数据。",
   "统计到达率：全天约 8547 条、整体到达率 95.0%，期间 2 次网络中断；明确“网络短时中断计入、外部停机剔除”的口径。",
   "绘制温度、湿度、可燃气体、土壤湿度的全天曲线，核对其日变化形态与当日晴热天气吻合。",
   "定位光照 BH1750 异常（白天多为 -1）：经比对串口与订阅链路，排除监控/订阅侧，判断为 ESP32 端 I2C 接线接触不良（模块本身正常）。",
   "输出测试报告、全天曲线与记录/分析脚本并入库。"],
  ["光照数据白天几乎全为无效值；本地不便 7×24 开机记录；需判断异常出在硬件端还是订阅/监控端。"],
  ["用云端持久记录器常驻接收，避免本地长时间开机；比对串口与订阅链路排除监控端问题，定位为硬件端 I2C 接触不良；统计时区分网络中断与外部停机。"],
  ["完成约 15 小时服务器侧记录与分析，四类数据合理、到达率 95.0% 达标，并定位光照问题为硬件端接线接触不良。"],
  ["复核性能与协议实验数据；待硬件接线修复后补测光照；把云端记录器升级为持久服务。"]),

 ("第 8 个工作日：性能数据复核、光照修复与记录器持久化", "2026年6月16日",
  ["今天复核已有性能与协议实验数据，配合硬件修复后补采光照数据并出曲线，并把云端记录器升级为持久服务，避免重启/欠费导致记录中断；同时梳理工程分析素材。"],
  ["复核协议开销、容量/吞吐、QoS × 丢包、公网延迟等实验数据与图表，确认与建模系数一致。",
   "硬件队友重新接稳 BH1750 后，服务器侧记录到夜间至清晨连续约 6 小时有效光照（0 → 约 305 lux），并绘制夜晨曲线，佐证模块本身正常。",
   "把云端记录器从临时单元升级为 systemd 持久服务（Restart=always、开机自启），避免服务器重启或欠费后记录中断。",
   "梳理工程分析素材（实时性、可靠性、可扩展性、成本、可维护性）。"],
  ["早期记录器为临时单元，服务器重启后会丢失；光照修复后需要一段干净曲线佐证 BH1750 本身正常。"],
  ["将记录器改为 systemd 常驻服务并开机自启；利用夜间至清晨稳定时段采集连续有效光照数据并出图。"],
  ["完成性能数据复核、光照夜晨曲线与记录器持久化，工程分析素材齐备。"],
  ["补做 MQTT 与 HTTP 开销对比实验，完善监控端，撰写并扩充课程设计报告。"]),

 ("第 9 个工作日：MQTT/HTTP 对比实验、监控端完善与报告撰写", "2026年6月17日",
  ["今天补做 MQTT 与 HTTP 的开销对比实验为协议选型提供量化依据；完善 Web 监控端（界面美化、选择订阅）；并撰写、扩充课程设计报告。"],
  ["编写 compare_mqtt_http.py，逐字节实测同一条数据经 MQTT 与 HTTP 上传的应用层开销（json/json-min/msgpack 各 200 次）：MQTT 比 HTTP 节省 65%~77%，协议头开销约为其 1/8。",
   "美化 Web 监控端为深色仪表盘，新增“选择订阅”功能（按节点、按数据类型或全部过滤，后端按 MQTT 通配符对该连接的推送过滤），直观体现主题订阅机制。",
   "厘清 QoS 选型思路：TCP 兜底连接内丢包、QoS 解决连接断开，故选 QoS 1 + 持久会话，并用 seq 去重替代 QoS 2。",
   "撰写并大幅扩充课程设计报告：实现过程详写、测试证据闭环、典型问题与解决过程、设计指标达成汇总表、硬件约束与安全性、工程化总结。"],
  ["需要量化“为什么选 MQTT 而不用 HTTP”；报告需补充实现细节、证据链与工程分析。"],
  ["做逐字节对比实验拿到真实数据支撑选型；按“测试方法—测试结果—结果分析”补充图表与典型问题闭环。"],
  ["完成 MQTT/HTTP 对比实验与数据归档、监控端“选择订阅”功能、以及课程设计报告的撰写与扩充。"],
  ["制作答辩 PPT 的协议与硬件部分，合并队友材料并提交全部成果。"]),

 ("第 10 个工作日：答辩 PPT 制作与成果提交", "2026年6月17日",
  ["今天完善答辩 PPT，合入硬件队友材料，统一接线口径，并整理、提交全部课程设计成果，完成项目闭环。"],
  ["完善答辩 PPT：新增“为什么选 MQTT（对比 HTTP 实测）”页、重构 QoS 论证页（讲清 TCP 与 QoS 分工）、把 2 万连接压测移到校园案例作为实验支撑、新增两页 ESP32 硬件页（组成与传感器接入、Wi-Fi 配网）。",
   "合并硬件队友的固件、测试记录与图片等 PR，统一接线口径（光照以串口/固件为准，即 BH1750）。",
   "核对课程设计报告、源代码、汇报 PPT、个人日志与测试附件齐备。",
   "用 Git 分模块提交并推送，保持过程留痕。"],
  ["硬件接线图中光照模块标注与实际（BH1750）不一致；多份材料需统一并提交。"],
  ["统一以串口/固件为准（BH1750），并提 Issue 请硬件队友更正接线图；分模块清点并提交全部成果。"],
  ["完成答辩 PPT、材料合并与成果提交，课程设计形成完整闭环。"],
  ["参加课程设计答辩演示。"]),
]

for title, date, content, tasks, probs, sols, res, plans in DAYS:
    h3(title)
    info("日期：" + date)
    label("1. 今日主要工作内容")
    for c in content:
        body(c)
    label("2. 本人完成的具体任务"); items(tasks)
    label("3. 遇到的问题"); items(probs)
    label("4. 解决方法"); items(sols)
    label("5. 今日成果"); items(res)
    label("6. 明日计划"); items(plans)
    sep()

# ===================== 个人总结 =====================
h3("个人总结")
label("1. 本人在课程设计中的主要贡献")
body("负责软件侧与项目流程推进，主要包括：需求分析与 MQTT 主题/消息格式设计；PC 模拟节点与 Web 监控端实现；网络负载与服务器配置成本建模；MQTT 5.0 方案升级与 EMQX 选型；协议开销抓包、容量/吞吐压测、QoS×丢包与跨公网延迟等性能实验；需求-成本计算器；MQTT 与 HTTP 开销对比实验；ESP32 室外长测的服务器侧记录与分析；课程设计报告与答辩 PPT 的撰写制作。同时主导 GitHub 协作流程与项目进度推进。")
label("2. 掌握的主要知识和技能")
body("MQTT 发布/订阅、主题设计、QoS 与持久会话；TCP 的可靠传输与重传；Wireshark 抓包与协议开销分析；EMQX 部署、压测与系统调优；Flask + SSE 实时 Web；网络负载与服务器容量、成本建模；Git 分支协作与 Issue/PR 评审。")
label("3. 遇到的主要问题及解决过程")
body("中期反馈要求“需求驱动、考虑成本”，据此建立通用计算模型与需求计算器；内存单一系数估算不准，补满负载爬坡实验改为双模型；QoS 与 TCP 的关系易混淆，用 QoS×丢包实验厘清 QoS 价值在断连；演示 Broker 一致性问题，统一改用本地 EMQX；光照数据异常，经比对定位为硬件端 I2C 接触不良并配合修复。")
label("4. 对系统设计的认识")
body("好的方案应由需求驱动、用实验量化、求一个有约束的最小成本，而非一味追求最强；软硬件采用同构接口、分层设计，并以完整的“需求—设计—实现—测试—分析”证据链支撑结论。")
label("5. 不足与改进方向")
body("现场供网稳定性不足、下行带宽随订阅端数放大、缺少数据持久化与告警、Broker 尚未启用认证与加密、硬件可靠性与 OTA 维护等，均为后续可改进的方向。")

# ===================== 表格 =====================
h3("附：测试与提交记录")
label("表 1  软件侧性能与协议测试结果")
table(["测试项目", "测试方法", "测试结果", "是否通过", "备注"],
      [["功能连通性", "双节点并发上传，按 seq 逐条对照", "到达率 100%（48/48），重连约 7 s，QoS 1 离线 12 条全部补发", "通过", "软件链路完整"],
       ["协议开销（抓包）", "Wireshark 抓不同 QoS/格式/Alias/版本", "单条 json/json-min/msgpack = 261.9/203.0/185.9 B，Alias 每条省 21.3 B", "通过", "作为带宽系数"],
       ["容量压测（EMQX）", "连接爬坡至 5 万", "5 万连接约 1.05 GB（15.75 KB/连），满载 38.76 KB/连（2.46×）", "通过", "选型按满载上界"],
       ["吞吐压测（EMQX）", "消息吞吐压测", "5 万条/秒约 3.2 核（约 1.9 万条/核·秒）", "通过", "对照 amqtt 千级即单核饱和"],
       ["QoS × 丢包", "0–15% 人为丢包测三种 QoS", "三种 QoS 到达率均 100%（TCP 重传），延迟随丢包率上升", "通过", "QoS 价值在断连"],
       ["跨公网延迟", "节点经公网发布、监控端订阅", "平均 44.7 ms、到达率 100%、断线重连 5.2 s", "通过", "满足 ≤ 1 s"],
       ["实景压测", "2 万连接、30 分钟、10 路转发", "连接零掉线，转发送达 99.96%，CPU 约 1.1 核、内存约 1.5 GB", "通过", "与计算器预测吻合"],
       ["MQTT vs HTTP", "同一条数据逐字节对比", "MQTT 比 HTTP 省 65%~77%，协议头约为其 1/8", "通过", "支撑协议选型"]],
      widths=[2.4, 4.0, 5.2, 1.6, 2.6])
label("表 2  关键指标实测")
table(["测试指标", "测试条件", "测试结果", "结果分析"],
      [["消息到达率", "按 seq 逐条统计实收/应收", "功能测试 100%，ESP32 真机 15 h 为 95.0%", "网络短时中断计入仍达标（≥ 95%）"],
       ["平均传输延迟", "跨公网 MQTT 发布/订阅", "平均 44.7 ms（P95 约 85 ms）", "远优于 ≤ 1 s，适合环境监测"],
       ["断线重连恢复", "断网后自动恢复并续传", "EMQX 约 5.2 s，节点功能测试约 7 s", "满足 ≤ 10 s"],
       ["单机容量", "EMQX 连接爬坡", "5 万连接约 1 GB 内存，远未触顶", "万级场景余量大，可横向扩展"],
       ["处理吞吐", "EMQX 消息压测", "5 万条/秒约 3.2 核", "单节点压力远低于此，瓶颈在带宽/存储"],
       ["连续运行稳定性", "2 万连接 30 min + 真机约 15 h", "零掉线、形成完整曲线", "长时间稳定达标"]],
      widths=[2.6, 4.6, 4.4, 4.2])
label("表 3  提交材料清单")
table(["材料名称", "是否提交", "备注"],
      [["课程设计报告", "☑ 是　□ 否", "docs/project_outputs/课程设计报告/"],
       ["源代码 / 仿真文件", "☑ 是　□ 否", "src/、scripts/、hardware/"],
       ["汇报 PPT", "☑ 是　□ 否", "期末答辩 Beamer 幻灯片"],
       ["个人日志", "☑ 是　□ 否", "本文件"],
       ["测试数据/抓包文件/截图", "☑ 是　□ 否", "captures/、docs/project_outputs/测试证据/"]],
      widths=[5.0, 4.0, 6.8])

out = "docs/team/daily/胡艺瀚_课程设计日志.docx"
doc.save(out)
print("saved:", out, "| 段落数:", len(doc.paragraphs), "| 表格:", len(doc.tables))
